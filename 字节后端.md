# 字节后端



## 算法

### 蛇形打印二叉树



先做这个题，层次打印二叉树，在正常的层序遍历上加一点实现蛇形遍历

![image-20251225154702812](E:\program\workspace\knowledge-base\assets\image-20251225154702812.png)



```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        List<List<Integer>> ans = new ArrayList<>();
        Deque<TreeNode>queue = new ArrayDeque();
        if(root==null) return ans;
        queue.add(root);
        //新增一
        boolean right2Left = false;
        
        while(!queue.isEmpty()){
            int size = queue.size();
            List<Integer>level = new ArrayList<>();
            for(int i=0;i<size;i++){
                TreeNode node = queue.poll();
                level.add(node.val);
                if(node.left!=null){
                    queue.add(node.left);
                }
                if(node.right!=null){
                    queue.add(node.right);
                }
            }
            //新增二
            if(right2Left){
                Collections.reverse(level);
            }
            right2Left = !right2Left;
            ans.add(level);
        }
        return ans;
    }
}
```

### 手撕：lc485，最大连续1的个数

![image-20251225160927404](E:\program\workspace\knowledge-base\assets\image-20251225160927404.png)

上面纯属脱裤子放屁的写法

![image-20251225161223577](E:\program\workspace\knowledge-base\assets\image-20251225161223577.png)

### 有序链表转换二叉搜索树

[109. 有序链表转换二叉搜索树 - 力扣（LeetCode）](https://leetcode.cn/problems/convert-sorted-list-to-binary-search-tree/description/)

其实不是很简单

### lc581：最短无序连续子数组

这个也难啊wok，一定要带个等于

![image-20251225174043016](E:\program\workspace\knowledge-base\assets\image-20251225174043016.png)

### 手撕LRU

重中之重，特别麻烦

[146. LRU 缓存 - 力扣（LeetCode）](https://leetcode.cn/problems/lru-cache/description/?envType=problem-list-v2&envId=2cktkvj)



**分别解释（为什么缺一不可）**

- **为什么需要 HashMap？（解决查找快的问题）**
  - “因为我们需要随机访问。当调用 get(key) 时，必须在 **O(1)** 时间内找到这个节点。如果是纯链表，查找需要遍历，复杂度是 O(N)，这太慢了。所以必须用哈希表来存储 Key -> Node 的映射。”
- **为什么需要链表？（解决顺序问题）**
  - “哈希表本身是无序的，无法知道哪个 Key 是‘最近使用’，哪个是‘最久未使用’。所以我们需要一个链表来维护**访问顺序**（Access Order）。最新的放在表头，最旧的放在表尾。”
- **（最关键点）为什么必须是【双向】链表？（解决删除快的问题）**
  - “这是为了实现 **O(1) 的节点删除**。
  - 当我们访问某个中间节点时，需要把它从当前位置删掉，移动到头部。
  - 如果是**单向链表**，虽然我拿到了当前节点，但我不知道它的**前驱节点（Pre Node）**是谁。想删除它，我必须从头遍历一遍去找到它的前驱，这又变成了 **O(N)**。
  - 而**双向链表**每个节点都持有 pre 指针，我不需要遍历，就能直接断开它和前驱的连接，实现真正的 **O(1) 删除**。”



### SQL题：SQL 查询题查询观看不同视频数量超过 100 个的用户。表字段包括：user_id, video_id, video_timestrap



```sql
SELECT 
    user_id
FROM 
    video_views
GROUP BY 
    user_id
HAVING 
    COUNT(DISTINCT video_id) > 100;
```



### 11.coding——N个点的最小遍历代价（n<=12)（dfs回溯+最短路算法）







## redis

### 1、redis在任何时候都是单线程的吗？



“Redis 不是纯单线程的。
在 **4.0** 引入了后台线程处理大键删除（UNLINK）和 AOF 刷盘；
在 **6.0** 引入了**多线程 I/O** 来解决网络吞吐瓶颈。
但它的**核心业务逻辑（命令执行）**依然是单线程的，这保留了 Redis 高效、无锁、原子性的优势。”



### Redis是内存数据库，有什么机制去防止数据丢失（RDB，AOF）



Redis 防止数据丢失主要依靠两种持久化机制：**RDB（快照）** 和 **AOF（追加日志）**。我在项目中是根据业务对数据可靠性的要求来配置的。

**1. RDB (Redis DataBase) —— 就像“照相机”**

**原理**：
RDB 是通过**快照**的形式，在指定的时间间隔内（比如 5 分钟），把内存里的所有数据生成一个二进制文件（dump.rdb）保存在磁盘上。
它的底层使用了 Linux 的 fork() 系统调用和 **Copy-on-Write（写时复制）** 技术。Redis 主进程会 fork 一个子进程，由子进程专门负责把数据写入磁盘，这样主进程可以继续处理客户端请求，不会被阻塞。

**优点**：
文件小、恢复速度非常快，适合做备份和灾难恢复。

**缺点**：
数据安全性较低。因为它每隔一段时间才保存一次，如果 Redis 突然挂了，我会丢失最后一次快照之后的所有数据（可能几分钟）。



**2. AOF (Append Only File) —— 就像“记账本”**

**原理**：
AOF 则是把 Redis 执行的每一条**写命令**（Set/Del等）都以文本日志的形式追加写入到磁盘文件里。
它有三种刷盘策略（appendfsync），最常用的是 **everysec（每秒刷盘）**。

**优点**：
数据非常安全。配置为 everysec 时，最多只丢失 1 秒钟的数据。

**缺点**：
文件体积很大（因为记录了所有流水账），而且重启时恢复数据很慢（需要把日志里的命令重放一遍）。为了解决文件过大，Redis 提供了 **AOF 重写（Rewrite）** 机制来压缩日志。

**3. 混合持久化（重点加分项，Redis 4.0+）**

**这也是我建议的方案**。
因为 RDB 丢数据多，AOF 恢复慢。从 Redis 4.0 开始，引入了**混合持久化**。
它的 .aof 文件里，前半段是 **RDB 格式**（快速恢复），后半段是 **AOF 格式**（增量数据）。
这样既保证了 Redis 重启时的**极速加载**，又保证了数据**尽量不丢失**（最多丢 1 秒）。

**总结**：
在我的**点餐系统**和**视频平台**中，为了兼顾性能和数据安全，我开启了 **Redis 4.0 的混合持久化**，并配置 AOF 策略为 everysec。这样即使服务器宕机，我也能保证绝大部分核心数据（如秒杀库存、视频状态）不丢失。

------



**面试官可能的追问（准备一下）：**

**Q: AOF 文件越来越大怎么办？**
**答**：Redis 会自动触发 **AOF 重写（Rewrite）**。它会创建一个新的 AOF 文件，去掉那些多余的中间操作（比如我对 num 加了100次，日志里有100条命令，重写后只保留 set num 100 这一条），从而大大压缩文件体积。这个过程也是由子进程完成的，不阻塞主线程。

**Q: 如果 RDB 和 AOF 同时开启，重启时加载哪个？**
**答**：Redis 会**优先加载 AOF 文件**。因为 AOF 记录的数据通常比 RDB 更完整、更新。





### Redis如何处理过期的情况



Redis 采用的是 **“惰性删除 + 定期删除”** 相结合的策略。

这是一个标准的面试回答结构，结合你的简历（尤其是**分布式锁**部分）来回答会非常出彩：

**1. 惰性删除（Lazy Deletion）—— “被动清理”**

**原理**：
当客户端去 **访问** 某个 Key 时（比如执行 GET 命令），Redis 才会先检查这个 Key 有没有过期。
如果过期了，Redis 就把它删除，并返回 nil；如果没过期，就正常返回数据。

**优点**：
**极大地节省 CPU**。因为只有在用的时候才检查，不会把 CPU 浪费在那些永远不会被访问的 Key 上。

**缺点**：
**浪费内存**。如果我有几百万个 Key 已经过期了，但一直没人去访问它们，它们就会一直占着内存不释放，造成内存泄漏。

**2. 定期删除（Periodic Deletion）—— “主动清理”**

为了解决惰性删除浪费内存的问题，Redis 会每隔一段时间（默认 100ms）执行一次后台清理任务。

**原理（随机抽查）**：
Redis **不会**遍历所有的 Key（那太慢了，会卡死），而是采用**随机抽取**的方式：

1. 从设置了过期时间的 Key 集合中，随机抽取 20 个 Key。
2. 删除其中已经过期的 Key。
3. 如果过期的 Key 比例超过 1/4（25%），就**重复**这个过程，直到比例下降。



![image-20251225151728334](E:\program\workspace\knowledge-base\assets\image-20251225151728334.png)



Redis有哪些具体的数据结构
介绍一下BitMap

### 介绍一下ZSet



在我的 **MiniBili 分布式视频平台**中，ZSet 发挥了核心作用，用于实现 **“每日热搜榜”**。

- **场景描述**：用户每天会搜索各种视频关键词，我们需要实时展示搜索量最高的前 10 个词。
- **具体设计**：
  - **Key 的设计**：我按天生成 Key，例如 hot_search:20251225。
  - **Score 的设计**：Score 就是“搜索次数”。
  - **操作逻辑**：
    1. 当用户搜索“Java教程”时，我调用 ZINCRBY hot_search:20251225 1 "Java教程"。如果词不存在，自动新增并设为1；如果存在，这就直接原子加 1。
    2. 当前端需要展示热榜时，我调用 ZREVRANGE hot_search:20251225 0 9 WITHSCORES，直接倒序取出前 10 名，效率极高。



**Q: 为什么不用红黑树（Red-Black Tree）而用跳表？**

- **答**：
  1. **范围查找（Range Query）更强**：ZSet 经常需要执行 ZRANGE（查排名区间）。在跳表里，只要找到区间起点，然后顺着底层链表遍历就行了，非常快；而红黑树做范围查找比较麻烦（中序遍历）。
  2. **实现简单**：跳表比红黑树简单得多，代码不易出错。

**Q: ZSet 的排名（Rank）是怎么算的？**

- **答**：跳表的每个节点通过记录“跨度（Span）”，可以在 O(log N) 的时间内算出当前元素的排名，不需要遍历整个链表



![image-20251225152222329](E:\program\workspace\knowledge-base\assets\image-20251225152222329.png)



### 提到了ZSet用于排行榜，如果相同分数但是我想让先达到这个分数的人排在前面，如何设计（加时间戳的综合score）



“我会采用**‘分数 + 时间权重’**的组合 Score 策略。
具体做法是利用 Score 的小数部分。
因为 Redis 的 ZSet 通常是倒序排列（ZREVRANGE），我们希望时间越早，分数越大。
所以我会用 **原始分数 + (未来常量时间 - 当前时间)** 作为最终 Score。

这样，同样的原始分数，**时间戳越小（越早），算出来的补码越大，最终总分就越高**，自然就排在前面了。



### ZSet的底层实现是什么样的，讲解一下



**作用**：维护数据的**顺序**。
**结构描述**：
跳表本质上是一个**多层的有序链表**。
最底层（Level 0）包含所有的元素。
在此之上，每隔几个元素就提取一个“索引节点”放到上一层（Level 1）。
再往上继续提取，形成金字塔结构。
**查询逻辑**：
查找时，从最顶层开始找，如果当前节点比目标值小，就往右走；如果大了，就往下沉一级。这样就跳过了大量的中间节点，实现了 **O(log N)** 的查找效率，和二分查找或红黑树相当





**3. 必考加分项：为什么用跳表而不用红黑树（B+树）？**

面试官一定会问这个问题，你需要给出这三个理由：

**理由一：范围查找（Range Query）更强**
ZSet 经常要执行 ZRANGE 或 ZREVRANGE（比如取排行榜前 100 名）。
在跳表中，只要通过 O(logN) 找到范围的起点，然后顺着最底层的链表指针一直向后遍历就可以了，非常快。
而在红黑树中，范围查找需要进行复杂的“中序遍历”，涉及到回溯父节点，效率和实现复杂度都不如跳表。

**理由二：实现简单**
红黑树的插入和删除需要进行复杂的**旋转（Rebalance）\**和\**变色**操作，代码非常难写且容易出错。
跳表的原理非常简单，插入节点时，只需要通过一个**随机函数**（抛硬币）来决定这个节点要“长”到第几层，维护成本极低。





## 消息队列

### 1、为什么选用ROcketMq？有没有考虑别的mq？其他mq的区别是什么？



我选择 RocketMQ 主要是基于三个方面的考虑：性能需求、业务功能匹配度以及技术栈的掌控力。

首先，在我的**秒杀项目**中，瞬时流量非常大。虽然 **RabbitMQ** 延迟极低，但它的吞吐量在万级左右，且基于 Erlang 开发，万一出问题我们很难深入源码排查，不符合我们 Java 团队的技术栈。

其次，虽然 **Kafka** 吞吐量是最高的，但它主要面向大数据日志场景。在我的**交易链路**中，我非常依赖**事务消息**来保证‘订单创建’和‘库存扣减’的最终一致性，通过 RocketMQ 的半消息机制，我可以完美解决分布式事务问题。此外，RocketMQ 对**消息重试**和**死信队列**的支持非常完善，这对金融级（点餐交易）业务的数据可靠性至关重要。

最后，RocketMQ 的底层设计比如 **mmap 零拷贝**和**磁盘顺序写**，保证了它在堆积大量消息时依然保持高性能。这就好比在我的**视频平台**项目中，哪怕用户集中上传视频导致消息积压，MQ 依然能稳定地让后端服务慢慢消费，不会把系统压垮。”



### MQ的分布式事务



RocketMQ 实现分布式事务的核心机制是 **“半消息（Half Message）” + “消息回查”**，这是一种**最终一致性**方案。

你需要把这个流程描述清楚（可以配合手势或画图思维）：

1. **发送半消息（Prepare）**：
   - 生产者（比如订单服务）先发一条“半消息”给 MQ。
   - 此时 MQ 收到了消息，但**消费者（比如库存服务）还看不见**，这条消息被存在一个特殊的内部 Topic 中。
2. **执行本地事务**：
   - MQ 确认收到半消息后，生产者开始执行本地事务（比如在数据库里插入订单）。
3. **提交或回滚（Commit/Rollback）**：
   - 如果本地事务成功，生产者发 Commit 给 MQ ——> MQ 把消息投递给消费者，消费者收到去扣减库存。
   - 如果本地事务失败，生产者发 Rollback 给 MQ ——> MQ 删除那条半消息，什么都不发生。



### 3、RocketMQ如何实现顺序消费？



**1. 生产者端（Producer）：保证“同类消息进同一个队列”**

- **原理**：RocketMQ 一个 Topic 下有多个 MessageQueue（默认4个）。默认发送是轮询的（乱序）。
- **实现**：我们需要使用 MessageQueueSelector。
- **做法**：在发送消息时，传入一个业务主键（比如 VideoID 或 OrderID）。通过 **Hash取模** 算法（id % queueSize），确保**同一个视频的所有状态变更消息，都发送到同一个 MessageQueue 中**。
  - *结果*：Queue-1 里全是视频A的消息，Queue-2 里全是视频B的消息。虽然 Queue-1 和 Queue-2 之间是并行的，但 Queue-1 内部是有序的。

**2. 消费者端（Consumer）：保证“一个队列单线程消费”**

- **原理**：普通的消费者监听器 (MessageListenerConcurrently) 是多线程并发消费的，可能会导致先到的消息被后启动的线程先处理完。

- **实现**：必须使用 **MessageListenerOrderly**（注意这个接口名，是得分点）。

- **做法**：

  - MessageListenerOrderly 会向 Broker 申请**分布式锁**，锁定当前要消费的那个 MessageQueue。
  - 在消费者客户端内部，它会保证**同一个 MessageQueue 同时只能被一个线程消费**。

- **结果**：即使消费者有多个线程，针对 VideoID=1001 的那个 Queue，也只能一个个挨着处理。

  

### 4、RocketMQ能实现死信队列吗？延迟消息呢？



**1. 关于死信队列（Dead Letter Queue, DLQ）**

RocketMQ 默认就开启了死信队列机制，它的核心逻辑是**“重试到达上限”**。

当消费端处理消息失败（抛出异常或返回 ReconsumeLater）时，RocketMQ 不会立刻丢弃消息，而是会进行自动重试。默认重试次数是 **16次**（时间间隔从 10秒到 2小时不等）。

如果重试了 16 次依然消费失败，Broker 就会把这条消息扔到一个特殊的 Topic 中，这个 Topic 的名字格式是 **%DLQ%你的消费组名称**。



**2. 关于延迟消息（Delayed Message）**

RocketMQ（开源版）不支持任意精度的延迟（比如“延迟 13分20秒”是不行的），而是内置了 **18 个特定的延迟级别**（1s, 5s, 10s, 30s, 1m, 2m, ... 2h）。发送消息时，只需要设置 setDelayTimeLevel(int level) 即可。



### 5、消息积压

首先，如果线上真的发生了严重积压，第一步必须是**快速判断原因**。我会先看监控，确认是消费者服务报错挂掉了，还是单纯的生产速度远超消费速度。如果是代码 Bug，那没得说，立刻回滚或者紧急修复上线。



第一是**批量消费**。
在我的**高并发校园点餐系统**中，数据库写入通常是瓶颈。我调整了 RocketMQ 的 consumeMessageBatchMaxSize 参数，每次拉取 **32条** 消息。然后在代码层面，我不是一条条插库，而是拼接成一条 SQL 进行**批量插入**。这极大地降低了数据库的 IO 频率，提升了消费吞吐量。

第二是**并发调整**。
针对普通业务，我会合理调整消费端的**线程池参数**（CorePoolSize），在机器资源允许的情况下，尽量让多线程并发去处理消息。

第三是**服务降级**。
在我的 **MiniBili 视频平台**中，我引入了 **Sentinel**。如果遇到流量高峰导致积压，我会优先保证“视频审核”和“转码”这些核心链路。对于“粉丝通知推送”或者“行为分析”这类非核心的消费者，我会通过 Sentinel 进行**限流或暂停消费**，把资源腾出来给核心业务，等高峰期过去后再慢慢补录。



### 消息丢失



因为我项目用的是 RocketMQ 的 **Spring Boot Starter**。

它的底层机制是依赖**异常**来判断状态的：
只要我的 onMessage 方法**正常执行结束**，框架就会自动提交 ACK，确认消费成功；
而如果业务逻辑出错，我会主动**抛出异常**，框架捕获后就会自动通知 MQ 进行重试。

所以我在代码中非常注意，在 catch 到业务异常后，一定会**再次 throw 出去**，绝不‘吞掉’异常，以此来保证消息不丢失。”



- **原生 API**：你自己写 try-catch，**必须显式 return** CONSUME_SUCCESS 或 RECONSUME_LATER。如果你忘了写 catch 直接崩了，或者返回了 null，消费者线程可能会卡住或行为异常。



## java八股



### 1、concurrenthashmap数据结构？



**逻辑分析**
面试官考察的核心是 **JDK 1.7 到 1.8 的底层演进**。你不能只说“它是线程安全的”，必须强调 **锁粒度的细化（从段到节点）** 以及 **数据结构的优化（引入红黑树）**。这体现了你对“高并发下如何减少锁竞争”的理解，非常符合字节跳动直播业务对高性能的要求。

**回答话术**
ConcurrentHashMap 在 JDK 1.7 和 1.8 的实现有很大不同，主要体现在 **锁的粒度** 和 **数据结构** 上。

在 **JDK 1.7** 中，它使用的是 **分段锁（Segment）** 机制。内部维护了一个 Segment 数组，每个 Segment 本质上是一把锁（继承 ReentrantLock），锁住一部分数据。虽然比 Hashtable 性能好，但 **并发度受限于 Segment 的数量**，且查询效率在链表长时是 O(n)。

在 **JDK 1.8** 中，做了重大优化。**结构上**，放弃了 Segment，直接使用 **数组 + 链表 + 红黑树**。**锁机制上**，改为了 **CAS + synchronized**。
**具体流程是**：如果插入位置没有冲突，直接使用 **CAS** 无锁插入，性能极高；如果发生冲突，才使用 **synchronized** 锁住当前的 **头节点**，锁粒度变得非常细。
此外，1.8 引入了 **红黑树**，当链表过长时自动转换，将查询时间复杂度从 O(n) 降低到 **O(log n)**，避免了哈希冲突下的性能退化。



### 2、synchronized与reentrantlock的区别

这个问题看似基础，但字节面试官主要考察你对 **“灵活控制锁”** 的理解。对于你的层级，不能只回答“用法不同”，重点要落在 **功能特性的差异** 和 **底层实现原理** 上。

1. **层面不同**：一个是 JVM 层面的“亲儿子”，一个是 JDK 层面的“工具类”。
2. **功能丰富度（关键得分点）**：面试官想听到 ReentrantLock 独有的 **公平锁、可中断、多条件变量（Condition）**，因为这些在复杂的直播/中间件场景（如 RocketMQ 源码）中非常重要。
3. **底层原理**：涉及 **ObjectMonitor（Monitor）** 与 **AQS（AbstractQueuedSynchronizer）** 的区别。
4. **性能观**：必须提到 JDK 1.6 以后 synchronized 做了 **锁升级（偏向、轻量、重量）** 优化，现在两者性能差异不大，官方推荐能用 synchronized 就用，除非需要特殊功能。

**回答话术**
这两者的区别主要体现在 **底层实现、功能丰富度** 和 **使用方式** 上。

首先，**底层实现不同**。synchronized 是 **JVM 层面** 的关键字，底层依赖对象的 **Monitor（监视器锁）** 实现；而 ReentrantLock 是 **JDK 层面** 的类，基于 **AQS（抽象队列同步器）** 实现，通过 CAS 和 volatile 状态变量来控制锁的获取。

其次，**使用方式不同**。synchronized 是 **隐式锁**，进入代码块自动加锁，退出自动释放，不容易死锁；ReentrantLock 是 **显式锁**，必须手动调用 lock()，且务必在 finally 块中调用 unlock() 释放，否则发生异常可能导致死锁。

最重要的是，**ReentrantLock 提供了 synchronized 不具备的高级功能**：

1. **支持公平锁**：synchronized 只能是非公平锁，而 ReentrantLock 构造时可以传入 true 实现 **公平锁**（先来后到）。
2. **支持响应中断**：通过 lockInterruptibly()，可以让等待锁的线程响应中断信号，避免死等，这在解决死锁时很有用。
3. **支持多个 Condition**：synchronized 只有一个 wait/notify 队列，而 ReentrantLock 可以绑定 **多个 Condition 对象**，实现更精细的分组唤醒（比如在你的 RocketMQ 源码理解中，生产者和消费者队列可以分开）。
4. **支持尝试非阻塞获取锁**：tryLock() 可以尝试获取锁，如果失败立即返回，适合像你的 **秒杀项目** 中这种需要“快速失败”的高并发场景。



### 3、为什么java要使用关键字和AQS两种方式实现不同的锁、

![image-20251225204851590](E:\program\workspace\knowledge-base\assets\image-20251225204851590.png)

1. **状态管理**：内部维护了一个 **volatile int state** 变量，代表共享资源的状态（比如 0 表示无锁，1 表示有锁）。
2. **线程排队**：维护了一个 **CLH 双向队列**，当线程通过 **CAS** 抢占资源失败时，会被封装成节点加入队列并阻塞等待。
3. **设计模式**：它采用了 **模板方法模式**，AQS 负责复杂的队列管理和线程唤醒，子类只需要实现 tryAcquire（尝试获取）和 tryRelease（尝试释放）等简单逻辑。



至于**为什么 Java 要同时保留 synchronized 和 AQS**，主要有三个原因：

第一，**功能场景的互补**。synchronized 只能实现 **独占锁**，且不可中断、不可设置超时。而 AQS 提供了更丰富的语义，它不仅支持 **独占锁**（ReentrantLock），还支持 **共享锁**（如 ReadWriteLock、Semaphore），以及 **条件队列（Condition）**，能应对更复杂的并发编排。

第二，**灵活性与控制力**。synchronized 是 **JVM 层面的隐式锁**，自动加锁释放，使用简单但灵活性差；AQS 是 **Java 代码层面的显式锁**，允许开发者手动控制锁的获取和释放（如 tryLock），支持 **公平锁** 和 **响应中断**，这在你的 **高并发秒杀项目** 中非常重要，比如通过 tryLock 实现“拿不到锁就立即返回”的快速失败机制，而 synchronized 做不到。

第三，**历史演进与兜底**。在 JDK 1.5 时，synchronized 性能较差，AQS 的出现是为了提供高性能的并发解决方案。虽然后续 JDK 1.6 对 synchronized 进行了 **锁升级** 优化，性能已相差无几，但 AQS 提供的 **非阻塞与高级同步语义** 是关键字语法无法替代的。





### 问了Java线程池的核心参数？



Java 线程池（ThreadPoolExecutor）主要包含 **7 个核心参数**，它们共同决定了线程池的运行策略：

1. **corePoolSize（核心线程数）**：线程池维持的最小存活线程数。即使是空闲的，通常也不会被销毁（除非设置了 allowCoreThreadTimeOut）。
2. **maximumPoolSize（最大线程数）**：线程池能容纳的最大线程数量。当队列满了，且当前线程数小于这个值时，会创建新线程（非核心线程）来应急。
3. **keepAliveTime（存活时间）**：非核心线程在空闲时的存活时间。超过这个时间没有任务，多余的线程会被回收。
4. **unit（时间单位）**：配合 keepAliveTime 使用。
5. **workQueue（任务队列）**：用于缓存待处理任务的阻塞队列。常见的有 ArrayBlockingQueue（有界）和 LinkedBlockingQueue（通常无界，有 OOM 风险）。
6. **threadFactory（线程工厂）**：用于创建新线程，通常用来**给线程命名**（如 “video-process-pool-%d”），方便线上排查故障。
7. **handler（拒绝策略）**：当线程池满了（线程达到 Max 且队列也满了）时，对新任务的处理策略。

**重点是它们的协作流程（面试加分点）：**
当一个新任务提交时：

1. 如果当前线程数 **< corePoolSize**，直接创建新线程执行任务。
2. 如果核心线程满了，将任务放入 **workQueue** 等待。
3. **关键点**：只有当 **队列满了**，且当前线程数 **< maximumPoolSize** 时，才会创建非核心线程来处理任务。
4. 如果队列满了，且线程数已达到 **maximumPoolSize**，则执行 **拒绝策略（Handler）**。



### 6、线程池的拒绝策略有哪些？

JDK 的 ThreadPoolExecutor 提供了 **4 种内置的拒绝策略**，同时我们也可以实现接口进行 **自定义**：

1. **AbortPolicy（默认策略）**：直接抛出 RejectedExecutionException 异常。
   - **适用场景**：这对业务感知要求很高，**必须明确知道任务失败了**（比如你的 **秒杀下单** 逻辑），以便上层业务进行事务回滚或给用户报错。
2. **CallerRunsPolicy（调用者运行策略）**：由 **提交任务的线程**（通常是主线程）自己去执行这个任务。
   - **核心优势**：这提供了一种 **“背压（Backpressure）”** 机制。当线程池满了，生产者线程被迫去执行任务，从而**降低了提交任务的速度**，给线程池喘息的机会，防止 OOM。
   - **适用场景**：适合对数据完整性要求高，且允许产生少量延迟的场景。
3. **DiscardPolicy（直接丢弃策略）**：直接把新任务丢掉，**不抛异常，也不处理**。
   - **适用场景**：适合 **非核心业务**，比如你的视频平台中的 **点赞数统计、非关键日志采集**，丢了也就丢了，不能影响主流程。
4. **DiscardOldestPolicy（弃老策略）**：丢弃 **队列中等待最久** 的那个任务，然后尝试把新任务塞进去。
   - **适用场景**：适合 **时效性要求高** 的场景，比如 **直播间实时的弹幕刷新** 或 **股票价格更新**，旧数据已经没有意义了，主要关注最新的。

**此外，在实际的核心业务（如支付/订单）中，我们通常不会直接使用这 4 种**，而是 **自定义策略（实现 RejectedExecutionHandler 接口）**。
例如在我的项目中，如果核心订单处理线程池满了，我会记录日志并 **将任务降级写入到消息队列（RocketMQ）或数据库** 中，由后续的补偿任务异步处理，**绝对保证数据不丢失**



### 介绍一下什么是线程池和传承new thread区别是什么

首先，**什么是线程池？**
它本质上是一种 **多线程处理形式**，把线程当作“昂贵的资源”预先创建好放在一个池子里管理。有任务来时直接分配给空闲线程，任务做完后线程不销毁，而是回到池子等待下一个任务。

**它与 new Thread() 的核心区别主要体现在三个方面：**

**第一，生命周期的开销（最核心的区别）。**

- **new Thread()**：每次执行任务都要经历 **“创建 -> 运行 -> 销毁”** 的全过程。
  - 在操作系统层面，创建一个线程需要 **从用户态切换到内核态**，分配栈内存（默认 1MB），这个成本非常高。如果任务本身执行很快，频繁创建销毁线程甚至是得不偿失的。
- **线程池**：它是 **“创建 -> 运行 -> 等待 -> 运行”**。核心是 **复用**，省去了频繁创建和销毁的系统开销，显著提升响应速度。

**第二，对系统资源的控制（稳定性）。**

- **new Thread()**：是 **不可控的**。如果瞬间来了 1 万个请求，它就会无限制地创建 1 万个线程，瞬间耗尽内存导致 **OOM（内存溢出）**，或者因为 CPU 都在做上下文切换导致系统卡死。
- **线程池**：是 **可控的**。它通过 maximumPoolSize 和 WorkQueue 限制了最大并发数。超过负荷的任务会排队或执行拒绝策略，**起到保护系统的作用**，保证系统在高压下不崩溃。

### 7、为什么要用线程池，或者说什么情况下用线程池？



第一，**降低资源消耗**。线程的创建和销毁属于 **重型操作**，需要进行系统调用和内存分配。通过 **重复利用** 已创建的线程，避免了频繁创建和销毁带来的 CPU 和内存开销。

第二，**提高响应速度**。当任务到达时，如果有空闲线程，任务可以 **无需等待线程创建** 就能立即执行。这对于 **低延迟** 敏感的业务（比如直播间的弹幕处理）至关重要。

第三，**提高线程的可管理性**。线程是稀缺资源，如果无限制地创建，不仅会消耗系统内存（可能导致 **OOM**），还会导致 CPU 频繁进行 **上下文切换**，反而降低系统吞吐量。线程池提供了 **统一分配、调优和监控** 的手段，能起到 **“限流”** 和 **“保护系统”** 的作用。



### 8、Java有哪几种线程池？

Java 通过 Executors 工具类提供了 **4 种常见的线程池**，它们适用于不同的场景，但底层都是基于 ThreadPoolExecutor 实现的：

1. **FixedThreadPool（固定线程数线程池）**
   - **特点**：核心线程数 = 最大线程数。线程池大小固定，任务多了就排队。
   - **场景**：适用于 **负载比较重但任务量稳定** 的场景，比如你的 **MiniBili 视频转码**，为了防止 CPU 过载，限制同时只能有 N 个转码任务。
2. **CachedThreadPool（缓存/可伸缩线程池）**
   - **特点**：核心线程数为 0，最大线程数为 **Integer.MAX_VALUE**（无限大）。队列使用 SynchronousQueue（不存储元素）。
   - **场景**：适用于 **执行时间极短、且任务量巨大** 的场景。来了任务就创建线程（或复用空闲的），不做停留。
3. **SingleThreadExecutor（单线程线程池）**
   - **特点**：只有一个线程在工作。
   - **场景**：适用于需要 **保证任务顺序执行** 的场景，比如 **数据库的顺序同步** 或 **日志的顺序记录**。
4. **ScheduledThreadPool（定时任务线程池）**
   - **特点**：支持定时及周期性任务执行。
   - **场景**：替代了传统的 Timer，用于 **心跳检测** 或 **数据定时上报**。

**但是（这里是加分转折点），在实际生产环境和我的项目中，我几乎不使用这 4 种快捷创建方式，而是手动 new ThreadPoolExecutor。**
这是因为 JDK 提供的这些默认配置存在 **严重的隐患**：

- **FixedThreadPool 和 SingleThreadExecutor**：允许的请求队列长度为 Integer.MAX_VALUE，可能会堆积大量请求，导致 **OOM（内存溢出）**。
- **CachedThreadPool**：允许创建的线程数量为 Integer.MAX_VALUE，可能会创建大量线程，导致 **CPU 100% 卡死** 或 OOM。
  **因此，明确指定队列大小和线程限制，才是高并发系统下的正确做法。**

### 9、ThreadLocal的实现原理？



10、ThreadLocal为什么在多线程情况是安全的？
11、ThreadLocal如果一个线程在读，一个在写，出现数据不一致怎么办？



### 12、Mybatis的缓存？

MyBatis 的缓存机制分为 **一级缓存** 和 **二级缓存**，它们的作用范围和生命周期不同：

第一，**一级缓存（SqlSession 级别）**。
它是 **默认开启** 的，基于 HashMap 实现。
它的作用域是 **同一个 SqlSession（一次数据库会话）**。在同一个会话中，如果执行相同的 SQL 查询，MyBatis 会直接从缓存返回结果，而不再查询数据库。
**失效条件**：一旦执行了 commit、rollback，或者进行了 **增删改（Update/Insert/Delete）** 操作，当前 SqlSession 的一级缓存会被立刻清空，以保证数据一致性。

第二，**二级缓存（Mapper/Namespace 级别）**。
它是 **默认关闭** 的，需要手动配置。
它的作用域是 **同一个 Mapper Namespace**，可以跨 Session 共享数据。也就是说，多个线程访问同一个 Mapper，可以共用缓存。

### 13、bean加载的过程？

Spring Bean 的加载过程非常复杂，但核心可以概括为 **四个大阶段**：**实例化、属性赋值、初始化、销毁**。

具体流程如下：

第一，**加载 BeanDefinition（图纸阶段）**。
Spring 启动时，会扫描 XML 或注解（如 @Component），解析出类的元信息（如类名、作用域、依赖关系），封装成 **BeanDefinition** 对象存入 Map 中。这是 Bean 的“身份证”。

第二，**实例化（Instantiation - 造壳子）**。
Spring 根据 BeanDefinition，通过 **反射** 机制（Constructor）创建一个 **原始对象**。
*注意：此时的对象是“空的”，依赖的属性（如 @Autowired 的 Service）还没注入。*

第三，**属性赋值（PopulateBean - 填里子）**。
Spring 通过 DI（依赖注入）机制，根据 BeanDefinition 中的信息，将引用类型的属性（其他 Bean）或值类型的属性填充进去。解决 **循环依赖** 的三级缓存逻辑也主要发生在这个阶段。

第四，**初始化（Initialization - 核心扩展点）**。
这是最复杂的阶段，分为三步：

1. **处理 Aware 接口**：如果 Bean 实现了 BeanNameAware 或 ApplicationContextAware，Spring 会把 Bean 的名字或容器本身传给它。
2. **执行 BeanPostProcessor 的前置处理（Before）**：允许用户在初始化前对 Bean 进行修改。
3. **执行初始化方法**：调用 @PostConstruct 注解的方法，或 InitializingBean 接口的 afterPropertiesSet 方法。
4. **执行 BeanPostProcessor 的后置处理（After）——【重点】**：
   - 这是 Spring **AOP（面向切面编程）** 的介入点。
   - 如果这个 Bean 需要被代理（比如加了事务 @Transactional），Spring 会在这里生成一个 **动态代理对象** 替换掉原始对象，最终放入容器的是代理对象。



14、BeanFactory 和 FactoryBean ？

5.jvm的作用



### 6.jvm垃圾回收，哪个垃圾回收算法性能更优



\- hashmap线程安全吗？哪些是线程安全的？
\- hashmap不同版本的区别
\- hash的底层数据结构
\- hashmap为什么使用红黑树？树化条件？
\- 为什么设置树化阈值为8？





## 数据库

数据库事务的特性ACID
介绍一下索引，索引和事务的关联

MySQL 执行一条SQL的请求过程

你参与了sql优化，可以讲讲具体是怎么优化的吗？有具体的流程吗？
\- explain执行计划重点关注字段？
\- 聚集索引和二级索引
\- mysql存储引擎（innodb），什么数据结构存储索引（B+树）
\- 为什么使用B+树而不用红黑树？
\- B+树增删改查的时间复杂度
\- 创建联合索引注意事项？索引失效场景？
\- 什么时候不符合最左前缀法则？
\- 最左前缀法则实现原理
\- 事务隔离级别，隔离产生的问题
\- mvcc实现原理，mvcc如何实现不可重复读和幻读？
\- mysql/mvcc如何防止幻读？ - 临键锁的实现原理，间隙锁的范围，开闭区间？

MySQL和redis怎么做同步





## 计网

1. TCP 为什么需要三次握手？两次不行吗？第三次握手的作用是什么？
2. TCP、UDP，TCP的可靠性
3. tcp和udp区别
   tcp有什么特点
   tcp怎么保证可靠性
   udp工作原理



## 操作系统

### 1.堆和栈、在操作系统的位置和作用



首先，在**位置**上，它们都位于**进程的虚拟地址空间**中。在常见的Linux内存布局里，**栈（Stack）**通常位于高地址空间，并向下增长；而**堆（Heap）**位于低地址空间（在未初始化数据段BSS之上），并向上增长。两者在物理上都存在于RAM中。

其次，在**作用和特性**上，我有三个层面的理解：

第一是**功能定位**：**栈**主要用于**控制程序执行流程**，它存储了函数调用的上下文，比如局部变量、函数参数和返回地址；而**堆**主要用于**存储动态数据**，即程序运行过程中动态申请的大块内存，在Java中主要就是对象实例。

第二是**管理方式与效率**：**栈**由操作系统自动分配释放，配合CPU的入栈出栈指令，**效率非常高**，但空间通常有限；**堆**则需要手动申请（或者由JVM的GC管理），在操作系统层面涉及复杂的内存分配算法（如空闲链表法），所以**效率相对较低**，但空间很大。

第三是**并发特性**（结合你的高并发简历）：这是我在做多线程开发时最关注的点。**栈是线程私有的**，每个线程有独立的栈空间，所以栈内变量不需要加锁，不存在线程安全问题；而**堆是进程内所有线程共享的**，我的项目中多个线程操作堆中的同一个对象时，就必须考虑**线程同步和锁机制**，比如我在项目中使用的Redis分布式锁或Java的Synchronized，本质上就是为了保护堆中共享数据的安全性。



1. **与Java的联系（关键点）：**
   Java的JVM内存模型其实是操作系统内存模型的一个抽象。JVM的堆也是向操作系统申请的一块内存（malloc/mmap）；Java线程一一映射到操作系统内核线程，因此Java虚拟机栈本质上就是操作系统线程栈的使用者。





### 2.内核态和用户态的切换过程



首先，**触发切换的场景**通常有三种：一是应用程序主动发起**系统调用**（比如我的项目中涉及的文件读取或网络IO）；二是程序运行中产生的**异常**（如缺页中断）；三是硬件产生的**外设中断**（比如网卡收到视频流数据）。

其次，**切换的具体过程**可以概括为“保存、切换、执行、恢复”四个步骤：

1. **保存现场**：CPU首先会暂停当前用户态程序的执行，将当前的**寄存器上下文**（包括程序计数器PC、通用寄存器等）保存到该线程对应的**内核栈**中。
2. **切换堆栈**：CPU会将栈指针（SP）从**用户态栈**指向**内核态栈**，这是实现权限隔离的关键。
3. **执行内核代码**：CPU根据系统调用号或中断号，去**中断向量表**（IDT）找到对应的内核处理函数地址并执行。
4. **恢复现场**：内核任务执行完后，再从内核栈中弹出之前保存的上下文，恢复寄存器状态，将CPU指令权交还给用户态程序。



### 3.中断的过程



![image-20251226142937796](E:\program\workspace\knowledge-base\assets\image-20251226142937796.png)



### 4.虚拟内存的作用



虚拟内存是操作系统在**物理内存**和**进程**之间建立的一层抽象。它给每个进程提供了一个假象：每个进程都认为自己独占一块连续的、巨大的内存空间。其核心作用主要有三点：

1. **隔离与保护（最重要的作用）：** 每个进程都有独立的页表，虚拟地址空间相互隔离。进程A无法访问进程B的内存，防止了一个程序的崩溃导致整个系统瘫痪（相比于早期的实模式）。
2. **内存扩展（利用磁盘）：** 当物理内存不足时，操作系统可以将暂时不用的内存页“换出”（Swap）到硬盘上，从而让程序能使用比实际物理内存更大的空间。

1. **内存管理简化：** 物理内存本身是碎片的，但通过虚拟内存映射（MMU），应用程序看到的是**连续**的地址空间，这极大地简化了编译器和链接器的编写难度。
2. **进阶特性（Copy-On-Write & mmap）：** 允许多个进程共享同一块物理内存（如动态链接库），并在需要修改时才复制副本。



**内存扩展：**

1. **分页机制（Paging）：** 操作系统把内存划分成固定大小的块，通常是 **4KB**，叫做**“页（Page）”**。
2. **冷热数据分离：** 程序的运行具有**局部性原理**，即在一段时间内，程序只频繁访问一小部分内存（**热数据**），剩下的部分可能很久不访问（**冷数据**）。
3. **置换过程：** 当物理内存不够用时，操作系统会启动**页面置换算法**（如LRU，最近最少使用算法），识别出哪些是“冷数据”页面，把它们的内容写到硬盘上的**Swap分区（交换空间）**，然后在页表中标记该页为“不在内存中”。这样物理内存就被腾出来了。
4. **缺页中断（Page Fault）：** 当程序后来试图访问被换到硬盘上的那个页面时，CPU发现该页不在内存，会触发**缺页中断**。操作系统捕获这个中断，从硬盘把数据读回内存（可能再次触发新的换出），然后程序继续执行。





### 5.内存管理的内容和作用



首先是**内存的分配与回收**。这是最基本的功能。操作系统需要维护内存的空闲状态（比如通过空闲链表），当进程启动或动态申请内存时（像C语言的malloc），系统为其分配物理内存；当进程结束时，系统负责回收资源，防止内存泄漏。

其次是**地址映射与转换**。这是现代操作系统的基石。程序操作的都是**逻辑地址**，内存管理单元（**MMU**）需要配合**页表**，将逻辑地址实时转换为**物理地址**。这使得编译器和程序员不需要关心具体的硬件内存分布。

第三是**内存保护与隔离**。这对于后端服务的稳定性至关重要。内存管理机制确保了每个进程都有独立的地址空间，互不越界。如果一个进程试图访问不属于它的内存（比如非法指针），硬件会触发异常，操作系统会直接终止该进程，从而保护其他进程和内核不受影响。

最后是**内存扩充与优化**。通过**虚拟内存技术**（请求分页和Swap），操作系统利用磁盘空间逻辑上扩展了内存容量。同时，通过**分页机制**（Paging），操作系统有效地解决了**内存碎片**问题，提高了物理内存的利用率。

作为Java开发者，理解这些有助于我更好地理解**JVM的内存模型**，比如JVM的堆其实就是向操作系统申请的一段连续虚拟空间，而JVM的垃圾回收（GC）本质上就是在用户态模拟操作系统的内存分配与回收逻辑。



### 6.epoll select（完全没复习到，直接说不会）



**背景：什么是IO多路复用？**
为了让一个线程能同时处理成千上万个网络连接（Socket），操作系统提供了IO多路复用机制。它的核心目标是：**不要让线程阻塞在一个连接上，而是监视所有连接，谁有数据来了就处理谁**



1. **Select（老旧笨重的方案）：**
   - **原理（轮询）：** 你的程序把1000个连接的文件描述符（FD）打包发给内核。内核**遍历**这1000个FD，看看谁有数据。如果都没数据，线程就阻塞。如果有数据，内核把这1000个FD全扔回来，但**不告诉你是哪几个有数据**。你的程序得在用户态**再遍历一遍**这1000个FD找到真正能读的那个。
   - **数据结构：** 使用**Bitmap（位图）**存储FD。
   - **缺点：**
     - **连接数限制：** 默认只能监控 1024 个连接。
     - **性能O(N)：** 连接数越多，轮询越慢。
     - **内存开销大：** 每次调用都要把所有FD从用户态拷贝到内核态。
2. **Epoll（高性能方案，Linux核心）：**
   - **原理（事件驱动）：** 就像在内核里建了个“派出所”。
     - **epoll_create**：在内核开辟一块空间。
     - **epoll_ctl**：有新连接时，注册到这里。底层用**红黑树**存储，查找速度O(logN)，而且**只需拷贝一次**，不需要每次调用都拷贝。
     - **epoll_wait**：关键点来了！当网卡收到数据，硬件中断会触发**回调函数**，直接把这个活跃的FD加入到一个**就绪链表（Ready List）**中。
     - 你的程序调用 wait 时，内核直接把这个**就绪链表**给你。你拿到的全是**真的有数据**的连接，不需要遍历无效连接。
   - **数据结构：** **红黑树**（存所有连接） + **双向链表**（存活跃连接）。
   - **优点：**
     - **无连接数限制**（仅受内存限制）。
     - **性能O(1)：** 性能只和**活跃连接数**有关，跟总连接数无关（百万连接里只有1个活跃，性能也极快）。
     - **零拷贝（近似）：** 利用mmap机制减少内核与用户空间的消息传递开销。

------



**回答面试官话术（标准满分版）：**

关于 select 和 epoll 的区别，我经过复盘深入理解后，认为本质上是**轮询模式**与**事件驱动模式**的区别，具体体现在以下三个方面：

第一是**操作机制与时间复杂度**。
**Select** 采用的是**线性轮询**机制。每次调用都需要遍历整个文件描述符集合，无论是否有数据，时间复杂度是 **O(N)**。在海量连接但活跃度低的场景下（比如长连接推送），性能会急剧下降。
**Epoll** 采用的是**事件驱动（Callback）\**机制。它在内核中维护了一个\**就绪链表**。当网卡收到数据触发中断时，通过回调函数将活跃的连接直接加入链表。应用程序只需要获取链表中的数据，时间复杂度是 **O(1)**（准确说是O(K)，K为活跃连接数），这使得它在高并发场景下性能非常稳定。

第二是**底层数据结构与拷贝开销**。
**Select** 使用**Bitmap**或数组存储连接，且每次调用都需要将整个FD集合从用户态**拷贝**到内核态，处理完后再拷贝回来，开销巨大。
**Epoll** 底层使用**红黑树**来管理所有监控的连接（增删改查效率高），并且通过 epoll_ctl 注册后，不需要重复拷贝FD集合。它还利用 **mmap** 技术映射内存，进一步减少了用户态与内核态的数据拷贝。

第三是**连接数限制**。
**Select** 受限于宏定义（FD_SETSIZE），默认通常只能监控 **1024** 个连接，不适合大规模并发。
**Epoll** 没有这个限制，能支持的连接数仅受限于**系统的最大打开文件句柄数**（通常是GB级内存对应数万甚至百万连接）。



### 生产者消费者模型



既然我们要深入底层，我非常愿意用操作系统经典的**信号量（Semaphore）\**和\**PV操作**来描述这个模型。这主要涉及**三个核心信号量**的协作：

首先，我们需要定义三个信号量：
一个是**互斥信号量 mutex**（初始值为1），用来保证对缓冲区的操作是互斥的，相当于一把锁；
一个是**同步信号量 empty**（初始值为缓冲区大小N），代表缓冲区里剩余的空位；
一个是**同步信号量 full**（初始值为0），代表缓冲区里已经存在的产品数量。

具体的操作流程如下：

对于**生产者**而言，它首先执行 **P(empty)**，申请一个空位资源。如果 empty 为0，生产者就会**阻塞**。申请成功后，它紧接着执行 **P(mutex)** 锁定缓冲区，进行数据的写入操作。写入完成后，执行 **V(mutex)** 释放锁，最后执行 **V(full)**，将产品数量加1，同时**唤醒**可能正在等待数据的消费者进程。

对于**消费者**而言，逻辑正好对称。它首先执行 **P(full)**，申请一个数据资源。如果 full 为0（即缓冲区空），消费者阻塞。申请成功后，执行 **P(mutex)** 锁定缓冲区，取出数据。完成后执行 **V(mutex)** 释放锁，最后执行 **V(empty)**，将空位数量加1，**唤醒**可能因为缓冲区满了而阻塞的生产者。

这里我特别想补充一个**死锁细节**：在代码实现中，**P(empty) 和 P(full) 必须放在 P(mutex) 之前**。如果生产者先抢到了互斥锁 mutex，然后再去 P(empty) 发现没有空位而阻塞，它就会带着锁睡眠，导致消费者永远拿不到锁来消费数据，最终导致**死锁**。这也是我在做多线程编程时非常注意的锁顺序问题。





### 怎么设计一个简单的生产者消费者模型，数据结构应该怎么设计



这是一个非常考察**代码落地能力**的问题。面试官不希望你只背概念，而是看你能不能把概念转化为具体的数据结构设计。这也是 Java `ArrayBlockingQueue` 的底层实现原理。

**解释：**

设计一个生产级的高性能生产者消费者模型，核心在于**数据结构**的选择和**并发控制**的设计。

1.  **数据结构选择：环形数组（Circular Array / Ring Buffer）**
    *   虽然链表（LinkedList）也可以，但在高性能场景下，**数组**优于链表。因为数组内存连续，对 **CPU 缓存（Cache Line）** 友好，且没有链表节点频繁创建销毁带来的 **GC 压力**。
    *   为了避免数组频繁的扩容和数据搬移，我们使用**环形复用**的逻辑。

2.  **核心变量设计：**
    *   ​    **容器：** `Object[] items` （固定大小的数组）。
    *   **指针：**
        *   `putIndex`：记录下一个存放数据的位置。
        *   `takeIndex`：记录下一个读取数据的位置。
        *   `count`：记录当前队列中元素的总数（用于判断空或满）。
    *   **锁与条件变量（关键）：**
        *   `Lock lock`：保证操作原子性（通常用 ReentrantLock）。
        *   `Condition notFull`：生产者等待队列不满的信号。
        *   `Condition notEmpty`：消费者等待队列不空的信号。

3.  **逻辑实现（双指针+取模）：**
    *   写入时：`items[putIndex] = x;`，然后更新指针 `putIndex = (putIndex + 1) % length`（或者用 `++` 判断越界归零）。
    *   读取时：`x = items[takeIndex];`，然后更新指针 `takeIndex = (takeIndex + 1) % length`。

---

**回答面试官话术：**

如果要设计一个简单的通用生产者消费者模型，我会参照 JDK 中 **ArrayBlockingQueue** 的设计思想，基于**有界环形数组**来实现。

首先，在**数据结构**的设计上，我会选择**固定大小的数组**作为底层容器。
之所以不选链表，是因为数组在内存中是连续的，对 **CPU 缓存**更友好，而且能避免在高并发下频繁创建节点导致的 **GC 压力**。

具体的**内部成员变量**我会这样设计：
我们需要维护两个**整型指针**：**`putIndex`** 指向下一个写入位置，**`takeIndex`** 指向下一个读取位置。
同时维护一个 **`count`** 变量原子地记录当前元素个数。
利用**取模运算**（或逻辑判断）让这两个指针在数组中循环移动，从而实现**环形缓冲（Ring Buffer）**的效果。

其次，在**并发控制**上，我会使用 Java 的 **Lock 机制**而不是简单的 `synchronized`。
具体来说，定义一个 **ReentrantLock** 互斥锁，并从锁中创建两个 **Condition 条件变量**：
一个是 **`notFull`**，用于阻塞**生产者**（当 `count == 数组长度` 时，生产者 `await`）；
一个是 **`notEmpty`**，用于阻塞**消费者**（当 `count == 0` 时，消费者 `await`）。

这里有一个非常重要的**实现细节**：在判断队列是否满或空时，我一定会使用 **`while` 循环**而不是 `if` 判断。
这是为了防止**虚假唤醒（Spurious Wakeup）**，即线程被唤醒后，状态可能仍然不满足条件（比如被其他线程抢先处理了），所以必须再次检查 `count` 的状态，确保程序的健壮性。





# 27日常实习字节后端开发二面 40min

1.项目介绍
2.为什么会选择做后端技术？
3.Redis的删除策略，如果内存满了的话？
4.讲一下LRU？
5.设计LRU优化Redis的删除策略？
6.讲一下MySQL的MVCC？
7.MVCC解决了大部分的幻读问题，哪些幻读问题是无法解决的？
8.延时消息是如何实现的？
9.RocketMQ的延时消息是如何实现的？
10.Kafka的并发量为什么那么高？



### ES

\- 简历中提到了ES，具体干了什么
\- ILM策略 - ES生命周期
\- 为什么进行Shrink操作
\- es中索引和文档的区别
\- es倒排索引机制




项目
concurrenthashmap在jdk1.7和jdk1.8的实现区别
cms和g1垃圾回收器
线程池
设计一个数据库引擎
设计一个定时任务执行器
两个文件海量数据找不同并写入第三个文件
算法题：奇升偶降链表变升序
