# ES复习



## 你的视频信息存放在 MySQL，搜索用 ES。你是如何保证 MySQL 和 ES **数据一致性**的？



#### 第一阶段：同步双写（即你现在的做法）

“在项目初期，为了快速实现，我采用了**同步双写**。在数据库事务中操作，如果 ES 写入失败则回滚（或者记录日志手动补偿）。这种方式实现简单，但存在性能问题和强耦合。”

#### 第二阶段：异步 MQ 解耦（进阶方案）

“为了提升性能，我会引入 **RocketMQ**（你简历里写了）。

1. 数据库写入成功后，发送一条消息到 MQ。
2. 专门的消费服务异步监听 MQ，负责将数据写入 ES。
3. **优势：** 即使 ES 挂了，消息还在 MQ 里，可以重试，实现了**最终一致性**，且显著降低了接口耗时。”



### 简历里写了‘智能推荐相似视频’，ES 是如何实现‘相似’的



#### 第一层：业务实现细节（你是怎么做的）

**你可以这样回答：**
“在我的视频平台中，‘相似视频’的推荐主要是基于**文本相关性（BM25算法）**实现的。

1. **索引设计：** 我在 videoName 上使用了 ik_max_word 细粒度分词，在 tags（标签）上使用了自定义的逗号分词器。
2. **核心逻辑：** 我使用了 ES 的 multiMatchQuery。当用户点击一个视频时，我会提取该视频的标题和标签作为‘隐式搜索词’，去匹配其他视频。
3. **排序权重：** 我将 ES 的相关性评分 _score 作为第一排序指标，配合视频的播放量、点赞量进行加权。这样就能在保证内容相关的前提下，把热门视频排在前面。”

#### 第二层：技术原理深度（ES 为什么觉得它们“相似”）

**面试官大概率会追问：ES 是根据什么算出这个相似度 _score 的？**

**你应该回答：**
“默认情况下，ES 使用的是 **BM25 算法**（它是 TF-IDF 的优化版）。它主要考虑三个维度：

- **TF (Term Frequency) 词频：** 关键词在标题中出现的次数越多，分值越高。
- **IDF (Inverse Document Frequency) 逆文档频率：** 如果一个词很稀有（比如‘字节’），它的权重就高；如果一个词很常见（比如‘视频’），它的权重就低。
- **Field Length Norm 字段长度规范：** 标题短的视频，匹配中的权重比标题超长的视频要高。

此外，我在代码中将 videoId 和 videoCover 设置了 "index": false，是为了节省空间并提高写入效率，因为这些字段不需要被搜索。”



### 用反射获取非空字段更新 ES，这解决了什么问题？

这是为了实现**增量更新**。如果不这样处理，直接传入一个 VideoInfo 对象，那些为 null 的字段会覆盖掉 ES 中已有的数据（导致数据丢失）。这样写能保证‘只更新修改了的部分’。”



### 你的分页用的是 from + size，如果我要看第 1000 页，你的代码会有什么风险？”

会存在 **Deep Paging（深度分页）** 问题。因为 ES 需要从各个分片收集前 N 条数据到协调节点再排序，翻页越深，内存压力和 CPU 消耗呈指数级增长。对于搜索建议或前几页推荐没问题，如果真的需要超深度翻页，我会改用 search_after。



### 你用了 IK 分词器，它有哪两种模式？对于‘抖音直播’这种新词（如：坤坤、精神小伙）层出不穷的场景，你是如何维护词库的？



在我的项目索引映射（Mapping）中，我为 videoName 字段指定了 ik_max_word。IK 分词器主要有以下两种模式：

1. **ik_max_word（最细粒度拆分）：**
   - 它会穷尽文本中所有可能的组合。比如‘抖音直播’，会被拆分为：‘抖音’、‘直播’、‘抖’、‘音’。
   - **适用场景：** 搜索。因为搜索时我们希望尽可能多地命中关键词，提高召回率。
2. **ik_smart（最粗粒度拆分）：**
   - 它会做语义上的‘最合理’切分。同样的‘抖音直播’，只会被拆分为：‘抖音’、‘直播’。
   - **适用场景：** 统计分析或不需要过度拆分的场景。”

------

#### 1. 初级方案：静态扩展词典（你知道原理）

“最简单的方法是修改 IK 插件目录下的 IKAnalyzer.cfg.xml 文件，手动在 extra_dict 中加入我新建的 .dic 词典文件。但这种方式**必须要重启 ES 节点才能生效**，在抖音直播这种需要实时更新词汇的场景下是不可接受的。”



### 你的代码里 tags 字段用的是自定义的 comma 分词器，为什么这里不用 IK？

- 对于视频标题（videoName），它是非结构化的文本，用户搜索习惯多样，所以需要 **IK 强力分词**。
- 对于标签（tags），它在发布时已经是结构化的（比如‘音乐,周杰伦,现场’），通常是以逗号分隔的准确词汇。如果用 IK 分词，可能会把‘周杰伦’拆成‘周’、‘杰伦’，反而破坏了标签的完整性。
- 所以我自定义了逗号分词器，保证**标签是什么，索引就是什么**，实现精准匹配。这体现了对不同业务数据采用不同的分词策略。”



### 你的ES在项目中是怎么使用的



在我的分布式视频平台中，Elasticsearch 核心负责**搜索检索**和**动态指标统计**两个模块。我主要从以下三个维度来实现和优化它：







**首先，在索引建模层面：**
我设计了一套自动化的 Mapping 初始化流程。针对视频标题，我选用了 ik_max_word 分词来提升搜索的查全率；而对于视频标签，为了保证标签的业务完整性，我自定义了 comma 分词器。同时，我将视频封面图等不需要搜索的字段设置为 index: false，通过减少倒排索引的大小来优化写入性能和存储成本。



“在项目初期，为了快速实现，我采用了**同步双写**。在数据库事务中操作，如果 ES 写入失败则回滚（或者记录日志手动补偿）。这种方式实现简单，但存在性能问题和强耦合。”



**其次，在数据写入与并发处理上：**
我有两套方案。对于视频信息的**增量更新**，我封装了一个基于 **Java 反射** 的工具类，它能自动剔除实体类中的空字段，保证 update 操作只覆盖有变动的数据。
对于播放量、收藏数这种**高频并发写入**，我重点解决了并发冲突问题。我没有采用普通的写入覆盖，而是编写了 **Painless 脚本**，将‘读取-修改-写回’的逻辑交给 ES 内部原子执行。这样既保证了数据的一致性，又避免了频繁并发修改导致的排他锁竞争。

![image-20251227204805661](E:\program\workspace\knowledge-base\assets\image-20251227204805661.png)



我在更新视频计数（如播放量、弹幕数）时，放弃了传统的‘查询-修改-写入’模式，转而采用 **ES Painless 脚本更新**。

这样做主要解决了两个问题：

1. **数据一致性：** 规避了并发环境下‘Read-Modify-Write’导致的计数丢失，将逻辑下沉到 ES 节点内部执行。
2. **性能优化：** 通过发送增量指令而非全量文档，减少了 IO 开销。

底层利用了 ES 的**乐观锁机制（Sequence Number）\**和\**内部重试机制**，确保了在秒杀、直播间爆火等高并发场景下，统计指标依然能够准确递增



**最后，在搜索实现上：**
我采用了 multiMatchQuery 跨字段匹配，并配合 HighlightBuilder 实现搜索结果高亮。为了保证系统的灵活性，我选择了**‘轻索引、重关联’**的策略：ES 只存储关联 ID，而用户昵称等易变信息在查询结果返回后，通过应用层在 MySQL 中通过 In 语句批量关联。这种设计在保证搜索效率的同时，极大地降低了数据同步的复杂度。

以上就是我在项目中使用 ES 的核心思路，重点解决了分词准确性、高并发下的数据一致性以及系统间的解耦问题。”