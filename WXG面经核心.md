# WXG面经



## 算法

### 1、区间合并

[56. 合并区间 - 力扣（LeetCode）](https://leetcode.cn/problems/merge-intervals/description/)

![image-20251224100241734](E:\program\workspace\knowledge-base\assets\image-20251224100241734.png)

### 2、k和链表反转

[25. K 个一组翻转链表 - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-nodes-in-k-group/description/)

![image-20251224103704783](E:\program\workspace\knowledge-base\assets\image-20251224103704783.png)

![image-20251224103822385](E:\program\workspace\knowledge-base\assets\image-20251224103822385.png)

### 3、合并k个有序单链表，核心就是归并排序 

![image-20251224110155097](E:\program\workspace\knowledge-base\assets\image-20251224110155097.png)

### 4、搜索二维数组

![image-20251224144032252](E:\program\workspace\knowledge-base\assets\image-20251224144032252.png)

### 5、kmp

[28. 找出字符串中第一个匹配项的下标 - 力扣（LeetCode）](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/solutions/575568/shua-chuan-lc-shuang-bai-po-su-jie-fa-km-tb86/)

### 6、字符串的括号匹配问题 

![image-20251224201615660](E:\program\workspace\knowledge-base\assets\image-20251224201615660.png)

### 7、力扣上的打家劫舍II（圆形住户，收尾相连）

![image-20251224201936411](E:\program\workspace\knowledge-base\assets\image-20251224201936411.png)

[213. 打家劫舍 II - 力扣（LeetCode）](https://leetcode.cn/problems/house-robber-ii/description/)

![image-20251224203607240](E:\program\workspace\knowledge-base\assets\image-20251224203607240.png)

[337. 打家劫舍 III - 力扣（LeetCode）](https://leetcode.cn/problems/house-robber-iii/)

![image-20251224205251592](E:\program\workspace\knowledge-base\assets\image-20251224205251592.png)



### 8、两个字符串A,B，将所有同时存在于A,B中的字母从A中剔除（HashMap）

```java
public static String removeCommon(String A, String B) {
    Set<Character> bSet = new HashSet<>();
    for (char c : B.toCharArray()) {
        bSet.add(c);
    }

    StringBuilder sb = new StringBuilder();
    for (char c : A.toCharArray()) {
        if (!bSet.contains(c)) {   // 只保留 B 中没有的
            sb.append(c);
        }
    }
    return sb.toString();
}
```

### 9、一棵二叉树，判断是否存在从根到叶子节点的路径，使得路径各节点值之和等于给定值（DFS）

```java
public boolean hasPathSum(TreeNode root, int targetSum) {
        if (root == null) return false;   // 空树
        return dfs(root, targetSum, 0);   // 初始 currentSum = 0
    }

    /**
     * DFS 辅助函数
     * @param node       当前节点
     * @param targetSum  目标和
     * @param currentSum 路径上已累加的和
     */
private boolean dfs(TreeNode node, int targetSum, int currentSum) {
    if (node == null) {          // 先判空，安全
        return false;
    }
    currentSum += node.val;      // 再累加

    // 到达叶子节点：一次性判断
    if (node.left == null && node.right == null) {
        return currentSum == targetSum;
    }

    // 左右子树只要有一条路径满足即可
    return dfs(node.left, targetSum, currentSum)
        || dfs(node.right, targetSum, currentSum);
}
```



### 堆排序

![image-20251224213658266](E:\program\workspace\knowledge-base\assets\image-20251224213658266.png)

[912. 排序数组 - 力扣（LeetCode）](https://leetcode.cn/problems/sort-an-array/submissions/686966022/)



### 前k个高频元素



[347. 前 K 个高频元素 - 力扣（LeetCode）](https://leetcode.cn/problems/top-k-frequent-elements/solutions/402568/qian-k-ge-gao-pin-yuan-su-by-leetcode-solution/)[347. 前 K 个高频元素 - 力扣（LeetCode）](https://leetcode.cn/problems/top-k-frequent-elements/solutions/402568/qian-k-ge-gao-pin-yuan-su-by-leetcode-solution/)



```java
对map排序

// 第一步：将 entrySet 转换为 
List List<Map.Entry<String, Integer>> list = new ArrayList<>(map.entrySet());

Collections.sort(list, new Comparator<Map.Entry<String, Integer>>() {
    @Override
    public int compare(Map.Entry<String, Integer> o1, Map.Entry<String, Integer> o2) {
        // 升序排序
        return o1.getValue().compareTo(o2.getValue());
    }
});

// 升序 
Collections.sort(list, (o1, o2) -> o1.getValue().compareTo(o2.getValue())); 
// 降序（值反过来比） 
Collections.sort(list, (o1, o2) -> o2.getValue().compareTo(o1.getValue()));
```

### 10、设计一个算法来统计海量数据中出现次数最多的前K个元素



**第一步：哈希切分（Divide） -> 解决内存不足问题**

由于数据太大，我们需要把大文件切分成很多小文件，保证每个小文件可以被加载到内存中。

- **操作**：逐行读取海量数据文件。

- **算法**：对每个元素（比如 IP 地址或搜索词 x）计算哈希值，然后取模：h(x) % M。

  - 

    ```
    MM
    ```

     是小文件的数量（例如切分成 1000 个小文件）。

    

  - 这样可以保证 **相同的元素一定会被分发到同一个小文件中**。

- **结果**：生成了 file_0, file_1, ..., file_999 共 M 个小文件。

**第二步：局部统计与局部 Top K（Conquer）**

依次处理每一个小文件。

1. **统计频率**：
   - 读取小文件 file_i 到内存。
   - 使用 **HashMap**（HashMap<String, Integer>）统计该小文件中每个元素的出现次数。
   - *注：因为第一步的哈希切分，这里的次数就是该元素在全局的的总次数。*
2. **筛选该文件的 Top K**：
   - 遍历这个 HashMap。
   - 使用一个 **大小为 K 的小顶堆（PriorityQueue）**（正是你刚才练习的代码结构）。
   - 维护当前小文件中频率最高的 K 个元素。
   - 处理完该小文件后，得到了该文件的“局部 Top K”。
3. **释放内存**：
   - 清空 HashMap 和堆，准备处理下一个小文件。

**第三步：全局归并（Merge）**

当所有小文件都处理完毕后，我们汇集了 

```
MM
```

 个小文件的 Top K 结果（总共有 

```
M×KM×K
```

 个数据）。

- **操作**：

  - 将这 

    ```
    M×KM×K
    ```

     个元素再次通过一个 **大小为 K 的小顶堆** 进行筛选。

    

  - 或者直接将这些元素放到一起排序。

- **结果**：堆中剩下的 K 个元素，就是全局出现次数最多的前 K 个



### 11、求比整数N小的所有正整数中，各位数字乘积最大者，如输入220，返回199

![image-20251225113459006](E:\program\workspace\knowledge-base\assets\image-20251225113459006.png)

![image-20251225113553928](E:\program\workspace\knowledge-base\assets\image-20251225113553928.png)

![image-20251225113606307](E:\program\workspace\knowledge-base\assets\image-20251225113606307.png)

public class Solution {
    public int findMaxProduct(int n) {
        String s = String.valueOf(n);
        int len = s.length();
        
```java
    long maxProd = -1;
    int ans = -1;

    // 1. 考虑位数少一位的情况 (例如 220 -> 99)
    if (len > 1) {
        StringBuilder sb = new StringBuilder();
        for (int k = 0; k < len - 1; k++) {
            sb.append('9');
        }
        int candidate = Integer.parseInt(sb.toString());
        long prod = getProduct(candidate);
        if (prod > maxProd) {
            maxProd = prod;
            ans = candidate;
        }
    } else {
        // 如果是个位数，比如 N=5，比它小的正整数最大乘积就是 N-1
        // 题目要求比 N 小的正整数，如果是 1，则没有正整数比它小（通常返回0或题目保证N>1）
        return n - 1; 
    }

    // 2. 遍历每一位进行贪心构造
    for (int i = 0; i < len; i++) {
        int currentDigit = s.charAt(i) - '0';
        
        // 如果当前位是0，无法减1（逻辑上需要借位，这在上一次迭代i-1时已经考虑过了），跳过
        if (currentDigit == 0) continue;

        // 构造候选数
        // 前缀：0 到 i-1 保持不变
        // 当前位：减 1
        // 后缀：填满 9
        StringBuilder sb = new StringBuilder();
        sb.append(s.substring(0, i)); 
        sb.append(currentDigit - 1);
        for (int k = i + 1; k < len; k++) {
            sb.append('9');
        }
        
        // 解析并计算乘积
        // 注意：如果 N=100, i=0 -> 099，parseInt 会自动处理成 99
        int candidate = Integer.parseInt(sb.toString());
        
        // 题目求比 N "小" 的正整数，如果构造出来是 0 (例如 N=1)，要排除
        if (candidate == 0) continue; 
        
        long prod = getProduct(candidate);
        
        // 更新策略：乘积更大，或者乘积相等但数字更大
        if (prod > maxProd) {
            maxProd = prod;
            ans = candidate;
        } else if (prod == maxProd) {
            if (candidate > ans) {
                ans = candidate;
            }
        }
    }
    
    return ans;
}

// 辅助函数：计算各位数字乘积
private long getProduct(int num) {
    if (num == 0) return 0;
    String s = String.valueOf(num);
    long prod = 1;
    for (char c : s.toCharArray()) {
        prod *= (c - '0');
    }
    return prod;
}

public static void main(String[] args) {
    Solution sol = new Solution();
    System.out.println(sol.findMaxProduct(220)); // 输出 199
    System.out.println(sol.findMaxProduct(100)); // 输出 99
    System.out.println(sol.findMaxProduct(51));  // 输出 49 (4*9=36 > 5*0=0)
}
```
}



12、最长公共字串

13、lru

14、删除单链表倒数第n的节点

15、分割回文串

16、lc53 最大子数组和

17、follow up：lc152 最大子数组乘积

18、归并排序

19、查找旋转数组

20、反转链表

21、lc15:三数之和

22、lc75:颜色分类  

23、二叉搜索树中第k大的元素

24、最长回文子串

25、二叉树根节点到叶子节点的所有路径和

26、



## 操作系统

### 1、什么是用户态和内核态？



**用户态和内核态本质上是 CPU 指令集权限的区别。**

- **内核态 (Ring 0)**：拥有最高权限，可以执行所有 CPU 指令，访问所有内存空间和 I/O 设备。操作系统内核、驱动程序都运行在这里。
- **用户态 (Ring 3)**：权限受限，只能访问受限的内存，不能直接操作硬件。我们平时写的 Java 应用程序、业务逻辑都运行在这里。
  这样划分主要是为了**安全性**和**稳定性**，防止用户程序随意修改内核数据导致系统崩溃。”

 **态的切换（触发时机）**
“用户态程序如果需要操作硬件（比如读写硬盘、发送网络包），就必须切换到内核态。通常有三种方式触发：

1. **系统调用 (System Call)**：这是用户进程主动发起的，比如调用 read()、write()、fork()。
2. **异常 (Exception)**：程序运行出错，比如缺页异常（Page Fault）、除零错误。
3. **外设中断 (Interrupt)**：硬件完成任务后的通知，比如网卡收到数据包、硬盘读写完成。”

“在做高并发优化时，我们都要尽量减少态的切换，因为**上下文切换（Context Switch）开销很大**。
当发生切换时，CPU 不仅要保存和恢复寄存器、程序计数器（PC）和内核栈，更重要的是会**破坏 CPU 高速缓存（Cache）**，特别是导致 **TLB（页表缓存）失效**，这会让接下来的内存访问变慢，降低 CPU 流水线的效率。”



### 2、什么是线程安全？



**当多个线程访问某个类（或对象）时，不管运行时环境采用何种调度方式或者这些线程如何交替执行，并且在主调代码中不需要任何额外的同步或协同，这个类都能表现出正确的行为**，那么这个类就是线程安全的。
本质上，线程安全问题都是由**全局变量**或**静态变量**等**共享资源**的竞争访问引起的。



### 3、操作系统层面，如何保证线程安全？说了锁和信号量，还有呢？



**一、互斥锁 (Mutex)**

**1. 核心概念**
“**互斥锁（Mutual Exclusion）**，就像是**只有一把钥匙的房间**。
它的核心语义是**‘独占’**和**‘所有权’**。

- **独占性**：同一时刻只能有一个线程持有锁，进入临界区（Critical Section）。
- **所有权（关键点）**：谁加的锁，必须由谁来解。线程 A 加的锁，线程 B 不能去解（除非是死锁恢复等特殊情况）。”



**二信号量 (Semaphore)**

**1. 核心概念**
“**信号量（Semaphore）**，就像是**有 N 个车位的停车场**。
它的核心语义是**‘控制并发数量’**和**‘信号通知’**。

- **计数器**：它维护一个整数值。线程执行 **P 操作**（Wait/Acquire）时计数减 1，执行 **V 操作**（Signal/Release）时计数加 1。
- **非所有权**：信号量没有‘所有者’的概念。线程 A 可以消耗一个许可，而线程 B 可以释放一个许可（常用于生产者-消费者模式）。”



**三 硬件层面的原子指令 (Atomic Instructions)**
这是操作系统实现锁的基础。在 x86 架构下，操作系统利用 CPU 提供的 **CAS (Compare And Swap)** 或者 **Test-and-Set** 指令（如 lock cmpxchg）。

- **原理**：它在硬件层面保证了‘读取-比较-修改’这三个动作是原子的，中间不会被中断。

- **结合简历**：这也正是 Java JUC 包中 AtomicInteger 的底层实现原理，也是我在简历中提到的**自研分布式锁**中用到的核心思想——通过**自旋（Spinlock）**代替阻塞，避免上下文切换。

  

**四 内存屏障 (Memory Barrier / Fence)**
操作系统和 CPU 通过内存屏障来解决**可见性**和**有序性**问题。

- **原理**：现在的 CPU 为了性能会乱序执行（Out-of-Order Execution），多级缓存（L1/L2/L3）也会导致数据不一致。内存屏障指令（如 mfence）强制 CPU 把缓存数据写回内存，并禁止屏障前后的指令重排序。
- **结合简历**：这也是我在学习 JVM 时理解的 volatile 关键字的底层实现，Java 的 volatile 在汇编层面就是加了 Lock 前缀指令（相当于内存屏障），从而保证线程安全中的可见性。



**五条件变量 (Condition Variable) 与 管程 (Monitor)**
锁只能解决‘互斥’，而条件变量解决的是**线程间的‘协作’与‘等待’**。

- **原理**：允许线程在不满足某个条件时挂起（释放 CPU），进入等待队列；当条件满足时，由另一个线程唤醒它。它通常和互斥锁配合使用。
- **结合简历**：Java 中的 synchronized 本质上就是**管程（Monitor）**模型，wait() 和 notify() 就是基于操作系统的条件变量实现的。





### 4、了解协程吗？你平时写一个程序，是多线程还是多进程？为什么不用多进程？



**1. 关于协程 (Coroutines)**

“**协程，简单来说就是‘用户态的线程’。**

- **核心区别**：传统的 Java 线程（Platform Thread）是和操作系统的内核线程 **1:1 映射**的，调度依赖操作系统，涉及到用户态和内核态的切换，开销较重（栈空间约 1MB）。
- **协程优势**：协程是 **M:N 模型**（M 个协程映射到 N 个内核线程），它的调度完全在**用户态**完成，不需要切换到内核。
  - **轻量**：栈空间极小（几 KB）。
  - **高效**：切换速度极快，特别适合 **IO 密集型**场景（如网关、RPC 调用）。
- **Java 的现状**：虽然我主要使用 Java 8/17 开发，但我关注到 JDK 19/21 推出的 **虚拟线程 (Virtual Threads)** 其实就是 Java 版的协程，它旨在解决高并发下线程资源耗尽的问题，这与 Go 语言的 Goroutine 思想是一致的。”



“**不过，我的项目中有一个例外场景使用了多进程。**
在我的**视频平台项目**中，进行**视频转码和分片**时，我使用的是 **FFmpeg**。
Java 本身处理音视频编解码的能力较弱，所以我是通过 Java 的 ProcessBuilder 去调用外部的 FFmpeg 程序的。
**这时候就是多进程模型**。
之所以这里接受多进程，是因为视频转码是极度 **CPU 密集型** 且独立的任务，不需要频繁的 IPC 通信，而且 FFmpeg 是 C 语言编写的高效程序，把它作为独立进程运行反而比嵌入 Java 更稳定（避免 Crash 搞挂主服务）。”



### 5、什么是内存碎片？如何解决？

**1. 什么是内存碎片？**

“内存碎片是指系统中虽然有足够的总空闲内存，但无法被有效利用的现象。它分为两类：

- **外部碎片 (External Fragmentation)**：
  - **现象**：空闲内存块分散在各处，就像瑞士奶酪的孔洞。虽然所有空闲块加起来很大，但没有一块连续的空间能满足当前的大对象分配请求。
  - **原因**：频繁的分配和释放大小不一的内存（分段管理容易产生此问题）。
- **内部碎片 (Internal Fragmentation)**：
  - **现象**：分配给进程的内存块比实际请求的要大，多余的部分被浪费了。
  - **原因**：为了对齐或者管理方便，内存分配器通常按固定大小（如 4KB、8KB）分配。比如我申请 10 字节，系统给我分配了 16 字节，那 6 字节就是内部碎片。”

**2. 操作系统层面如何解决？**

“在 OS 层面，主要通过以下几种机制解决：

- **分页机制 (Paging) —— 解决外部碎片**：
  这是最根本的手段。通过页表将逻辑地址映射到物理地址，**物理内存不再需要连续**。只要物理内存还有空闲的页框（Frame），就能分配给进程，彻底消除了外部碎片。
- **内存压缩/紧凑 (Compaction)**：
  类似磁盘碎片整理。操作系统移动内存中的活动对象，把空闲空间挤在一起。但这对 CPU 消耗极大，通常不实时进行。
- **高级内存分配器 (Linux Kernel)**：
  - **伙伴系统 (Buddy System)**：用于解决**外部碎片**。它把内存按 2 的幂次方划分，释放时自动合并相邻的块。
  - **Slab 分配器**：用于解决**内部碎片**。它在伙伴系统之上，专门处理小对象的分配（如 task_struct），按对象大小分配固定坑位，填满每一个页。”

**3. 结合简历：Java/JVM 如何解决碎片？**

> *这是体现你 Java 基础扎实的关键点。*

“在 Java 虚拟机中，内存碎片问题主要由 **GC（垃圾回收器）** 来处理：

- **标记-清除算法 (Mark-Sweep)**：
  比如 CMS 垃圾回收器。它只标记并回收垃圾，不移动对象，这**会产生严重的外部碎片**。一旦碎片过多，就会触发 Full GC 进行整理。
- **标记-整理算法 (Mark-Compact)**：
  比如 Serial Old 或 Parallel Old。回收后会将存活对象向一端移动，**解决了外部碎片**，但移动对象时需要‘Stop The World’。
- **复制算法 (Copying)**：
  比如新生代的 Survivor 区。每次只保留一半内存，将存活对象复制到另一半，天然没有碎片。
- **TLAB (Thread Local Allocation Buffer)**：
  为了解决多线程竞争和内部碎片，JVM 为每个线程预先分配一小块专有内存（TLAB），在分配小对象时非常高效且无锁。”



### 6、进程通信的方式有哪些？



在操作系统中，由于进程间内存是隔离的，所以需要 IPC 机制。我把常见的 6 种方式按在**Java 后端开发中的重要程度**分为三类来介绍：

**第一类：分布式与网络通信核心 —— 套接字 (Socket)**

这是目前**最通用、最重要**的方式，因为它不仅支持本地进程通信，更支持**跨网络/跨主机**通信。

- **原理**：通过 IP 和端口进行端到端的数据传输。
- **结合简历**：我的整个**微服务架构**都是建立在 Socket 之上的。
  - **服务调用**：OpenFeign 底层基于 HTTP，而 HTTP 底层就是 TCP Socket。
  - **中间件连接**：我的 Java 应用连接 MySQL、Redis、RocketMQ，本质上都是建立了 Socket 连接。

**第二类：高性能通信核心 —— 共享内存 (Shared Memory)**

这是**效率最高**的 IPC 方式。

- **原理**：操作系统拿出一块物理内存，映射到两个进程的虚拟地址空间中。这样进程 A 写数据，进程 B 立刻就能看到，**完全避免了数据在用户态和内核态之间的拷贝**。
- **结合简历（亮点）**：
  在我的**在线视频平台**项目中，涉及到大文件传输和视频流处理。
  虽然 Java 是基于堆内存的，但 Netty 和 Java NIO 中使用的 **Direct Memory (直接内存)** 以及 **mmap (内存映射)** 技术，其底层思想就是利用共享内存/零拷贝技术，减少数据拷贝次数，大幅提升了视频 I/O 的吞吐量。

**第三类：单机基础通信**

这几种主要用于单机环境下的控制和简单数据交换：

1. **管道 (Pipe)**：
   - **原理**：半双工的通信方式，数据单向流动。
   - **结合简历**：我在项目中使用 **FFmpeg** 进行视频转码时，Java 通过 ProcessBuilder 调用 FFmpeg 进程，获取它的输出流（Stdout）来判断转码进度，这中间就是通过**匿名管道**传输数据的。我们在 Linux 下常用的 ps | grep 也是管道。
2. **消息队列 (Message Queue)**：
   - **原理**：内核中的链表。
   - **对比**：操作系统层面的消息队列主要用于本机轻量级通信。而我在项目中使用的 **RocketMQ** 是**应用层**的分布式消息队列，它解决的是分布式系统间的**解耦、削峰填谷**，虽然名字一样，但解决的问题规模不同。
3. **信号 (Signal)**：
   - **原理**：用于通知进程某个事件发生（如 kill -9）。
   - **场景**：比如 Spring Boot 应用在关闭时会监听 SIGTERM 信号，触发 **Graceful Shutdown（优雅停机）** 逻辑，确保处理完当前的请求再关闭。
4. **信号量 (Semaphore)**：
   - 主要用于进程间的**同步**，控制对共享资源的访问（前面线程安全题已详细讨论）。”

------

**面试官追问预判**

1. **追问：共享内存这么快，有什么缺点吗？**
   - *回答：* 只有数据，没有同步机制。如果两个进程同时写，数据会乱。所以共享内存通常必须配合**信号量**或**互斥锁**一起使用。
2. **追问：Socket 通信中，TCP 和 UDP 的区别？**
   - *回答：* TCP 面向连接、可靠（视频上传、订单系统）；UDP 无连接、不可靠但快（视频直播、即时通讯）。
3. **追问：你刚才提到 RocketMQ 和 OS 消息队列的区别，那 RocketMQ 底层是用什么通信的？**
   - *回答：* RocketMQ 的 Broker 和 Client 之间是通过 **Socket (Netty 框架)** 进行 TCP 通信的。



### 7、了解copy on write(cow)技术么？fork()的细节是怎么样的？

**1. 什么是 fork() 的细节？**

“fork() 是 Linux 中创建新进程的核心系统调用。
在早期的 Unix 中，fork() 会把父进程的所有资源（包括物理内存数据）完整拷贝一份给子进程，这非常慢且浪费内存。

**现代 Linux 的 fork() 进行了优化：**

1. **复制页表，不复制内存**：当执行 fork() 时，内核只复制父进程的**页表 (Page Table)** 和 task_struct（进程描述符）给子进程，而**不复制物理内存中的数据**。
2. **共享物理页**：此时，父子进程的页表指向**同一块物理内存**。
3. **标记只读**：内核会将这些共享的内存页在页表中标记为**‘只读’ (Read-Only)**。
4. **返回 PID**：fork() 调用迅速结束，父进程返回子进程的 PID，子进程返回 0。

所以，现代 fork() 的速度极快，几乎是 O(1) 的，仅受限于页表的大小，而不是内存的大小。”



**2. 什么是 Copy On Write (COW)？**

“**写时复制 (COW)** 是一种延迟分配资源的策略，紧接着 fork() 发生：

- **读取时**：只要父子进程都只进行‘读’操作，它们就一直共享同一份物理内存，没有任何额外开销。
- **写入时（触发机制）**：
  1. 当任意一个进程（父或子）尝试**修改**某个数据页时。
  2. CPU 的 **MMU (内存管理单元)** 发现该页表项是‘只读’的，会触发一个 **缺页中断 (Page Fault)** 异常。
  3. **内核捕获中断**：内核发现这是因为 COW 机制触发的，就会**分配一个新的物理内存页**，将旧页面的数据**拷贝**过去。
  4. **更新页表**：将执行修改的进程的页表指向这个新页，并将权限修改为‘可读写’。
  5. **恢复执行**：进程重新执行写入操作，此时写入的是自己的私有副本。

**核心优势**：只有真正被修改的数据才会被拷贝，极大节省了内存和 CPU 时间。”





“结合我的**Redis**项目经验，Redis 的 **RDB 持久化 (bgsave)** 就是 Copy On Write 技术的典型应用。

- **过程**：当 Redis 执行 bgsave 时，主线程会 fork() 出一个子进程。
- **利用 COW**：
  - 子进程刚刚创建时，和主进程共享内存，它负责将内存数据写入磁盘（RDB 文件）。
  - 在子进程写入磁盘的期间，主进程依然可以处理客户端的 **写请求**。
  - 得益于 COW，主进程修改数据时，操作系统会为被修改的数据页分配新内存，而子进程依然读取的是 fork 瞬间的‘旧数据’（快照）。
- **性能影响**：这也解释了为什么 Redis 在 RDB 持久化期间，如果写流量很大，内存占用可能会瞬间增加（最坏情况下翻倍），因为被修改的页都被复制了一份。”



### 8、分段内存管理和分页内存管理



分段和分页本质上都是为了解决**内存碎片**和实现**虚拟内存**而设计的非连续分配方式。

**分页**是将物理内存和逻辑内存切分成**固定大小**的块（通常是 4KB）。

- **机制**：逻辑上叫‘页 (Page)’，物理上叫‘页框 (Frame)’。操作系统维护一张**页表 (Page Table)** 来做映射。

**分段**是按照程序的**自然逻辑结构**来划分内存的。

- **机制**：比如一个进程分为**代码段、数据段、栈段**。段的大小是不固定的，由程序逻辑决定。

. 对比总结（面试官最爱听的表）

“如果做一个对比的话：

| 特性       | 分页 (Paging)                  | 分段 (Segmentation)                |
| ---------- | ------------------------------ | ---------------------------------- |
| **目的**   | 提高物理内存利用率（系统视角） | 满足代码逻辑和保护需求（用户视角） |
| **大小**   | **固定** (如 4KB)              | **不固定** (动态分配)              |
| **碎片**   | **内部碎片**                   | **外部碎片**                       |
| **可见性** | 对程序员隐形                   | 程序员/编译器可见                  |

**进阶：现代操作系统是怎么做的？（Linux）**

“在实际的现代操作系统（如 Linux）中，通常使用的是**段页式管理**。
不过 Linux 比较特殊，它为了移植性，把‘分段’的功能弱化了（基地址都设为 0，相当于全内存平坦模式），主要依赖**分页**来管理内存。
这样做既保留了分段的权限检查（Ring 0 / Ring 3），又利用了分页的高效内存管理，避免了外部碎片。”



## Linux

### linux怎么查看cpu使用情况  

### top命令输出的有哪些参数，一般会看哪些 



“查看 CPU 最常用的肯定是 top。但在 top 界面里，我主要关注三块内容：

- **Load Average (平均负载)**：
  - 看 1、5、15 分钟的趋势。如果 Load > CPU 核数，说明系统过载了。
- **CPU 使用率的具体分类 (的关键指标)**：
  - **us (User)**：用户态高。通常是业务逻辑复杂，或者出现了死循环。
  - **sy (System)**：内核态高。通常是**频繁的上下文切换**（比如大量线程竞争锁）或者频繁的文件 I/O 操作。
  - **wa (IO Wait)**：**这个很重要**。如果 wa 很高，说明 CPU 没干活，都在等磁盘 I/O。在我的**视频平台项目**中，如果转码服务卡顿，我首先会看这个指标。
- **si (Soft IRQ)**：软中断。如果网卡流量特别大（比如视频流突发），这个指标会升高。



**全局/历史趋势：vmstat**

“如果 top 看不出大概，我会用 vmstat 1（每秒刷新）来分析：

- **r (Running)**：运行队列长度。如果这个值长期大于 CPU 核数，说明 CPU 不够用了。
- **cs (Context Switches)**：每秒上下文切换次数。如果这个值达到几十万，配合 sy 升高，说明我的 Java 线程池可能开太大了，或者锁竞争太激烈。”



## 计网



### 1、讲下TCP拥塞控制

**2. 四大算法流程（核心机制）**

“TCP 维护一个**拥塞窗口 (cwnd, congestion window)**，算法主要分为四个阶段：

- **1. 慢启动 (Slow Start)**：
  - 虽然叫慢启动，其实增长很快。连接刚建立时，cwnd 初始化为 1，每收到一个 ACK，cwnd 加倍（1, 2, 4, 8... **指数增长**）。
  - 这会一直持续到达到 **ssthresh (慢启动阈值)**。
- **2. 拥塞避免 (Congestion Avoidance)**：
  - 当 cwnd 超过阈值后，为了避免网络瞬间拥塞，改为**线性增长**（每经过一个 RTT，窗口 +1），小心翼翼地探测网络上限。
- **3. 拥塞发生（关键点：如何检测拥塞？）**：
  - **情况 A：超时重传 (Timeout)**（严重拥塞）：
    - TCP 认为网络死掉了。ssthresh 设为当前窗口的一半，cwnd **重置为 1**，重新进入慢启动。**（一夜回到解放前，性能损耗巨大）**
  - **情况 B：快重传 (Fast Retransmit)**（轻微拥塞）：
    - 如果你收到了 **3 个重复的 ACK**（说明中间丢了一个包，但后续的包到了），TCP 认为网络还没完全堵死。
    - 此时立即重传丢失的包，不等待超时。
- **4. 快恢复 (Fast Recovery)**：
  - 配合快重传使用。发生快重传后，ssthresh 减半，但 cwnd **不回 1，而是设置为减半后的值**，直接进入**拥塞避免**阶段。
  - 这样可以跳过慢启动，保证传输效率。”

------



### 2、tcp udp区别

![image-20251223214603511](E:\program\workspace\knowledge-base\assets\image-20251223214603511.png)

### 3、TCP相关有了解吗，**说一下三次握手和四次挥手**

#### 第一部分：三次握手 (Connection Establishment)

“TCP 是面向连接的、可靠的传输协议，三次握手的核心目的不是为了‘握手’，而是为了**同步双方的初始序列号 (ISN, Initial Sequence Number)**，防止历史连接初始化了连接

**具体流程：**

1. **SYN**：客户端发送 SYN=1, seq=x，进入 **SYN_SENT** 状态。
2. **SYN + ACK**：服务端收到后，回复 SYN=1, ACK=1, ack=x+1, seq=y。服务端进入 **SYN_RCVD** 状态。
3. **ACK**：客户端检查 ack 正确后，发送 ACK=1, ack=y+1, seq=x+1。此时客户端进入 **ESTABLISHED**，服务端收到后也进入 **ESTABLISHED**。

![image-20251223211309279](E:\program\workspace\knowledge-base\assets\image-20251223211309279.png)

**【进阶：为什么是 3 次？2 次行不行？】**
这是为了**防止‘已失效的历史连接请求’突然又传到了服务端**。

- 如果只有 2 次：假设客户端发出的第一个 SYN 包滞留了，客户端超时重发并建立了新连接，等新连接结束后，那个**滞留的旧 SYN** 到了服务端。服务端以为是新连接，直接回复 ACK 并建立连接，这就产生了资源浪费。
- 有第 3 次：服务端收到旧 SYN 回复 ACK 后，客户端会发现这个 ACK 的序列号不对（不是我刚才发的），就会发送 RST 报文拒绝，从而避免错误连接



#### 第二部分：四次挥手 (Connection Termination)

“TCP 连接的断开需要 4 个步骤，因为它支持**全双工**模式。

**具体流程：**

1. **FIN**：客户端（主动关闭方）发送 FIN=1，进入 **FIN_WAIT_1**。表示‘我没有数据要发给你了’。
2. **ACK**：服务端收到 FIN，回复 ACK。客户端进入 **FIN_WAIT_2**。此时 TCP 处于**半关闭状态**：客户端不能发数据，但能收数据。
3. **FIN**：服务端处理完剩下的数据后，发送 FIN=1，进入 **LAST_ACK**。
4. **ACK**：客户端收到 FIN，回复 ACK，进入 **TIME_WAIT** 状态。服务端收到 ACK 后进入 **CLOSED**。

![image-20251223211047837](E:\program\workspace\knowledge-base\assets\image-20251223211047837.png)



#### 第三部分：核心考点 —— TIME_WAIT 状态（重点！）

> *面试官问四次挥手，其实 80% 是想问 TIME_WAIT。一定要主动讲！*

“在四次挥手最后，主动关闭方（通常是客户端，但在 Web 服务中，**服务端**往往是主动关闭方）会进入 **TIME_WAIT** 状态，持续 **2MSL**（报文最大生存时间，Linux 默认 60秒）。

**为什么需要 TIME_WAIT？**

1. **保证可靠关闭**：如果最后一个 ACK 丢了，服务端会重发 FIN。客户端必须维护这个状态，才能重新发送 ACK。如果客户端直接 CLOSED，服务端重发 FIN 时会收到 RST 报错。
2. **防止旧报文混淆**：等待 2MSL 可以确保本连接产生的所有报文都在网络中消失，避免干扰下一个使用相同端口的新连接。



### 4、四次挥手为什么是四次，不是三次



**核心回答：全双工与半关闭**

“四次挥手之所以是四次，根本原因在于 **TCP 是全双工（Full Duplex）的协议**，它的发送和接收通道是**相互独立**的。

**具体原因分析：**

1. **方向独立关闭**：
   当客户端发送 FIN 时，仅仅代表**客户端没有数据要发给服务端了**（客户端进入半关闭状态），但客户端依然可以**接收**服务端发来的数据。
2. **ACK 与 FIN 的分离（关键点）**：
   - **第 2 次挥手（ACK）**：当服务端收到客户端的 FIN 包时，**内核的 TCP 协议栈**会立即自动回复一个 ACK，告诉客户端：“我知道你想断开了，但我还没准备好。”
   - **中间的等待期**：此时，服务端应用程序可能还有**剩余的数据**（比如缓冲区的视频流、未发送完的响应报文）需要继续发送给客户端。应用程序需要处理完手头的工作，才会主动调用 close() 方法。
   - **第 3 次挥手（FIN）**：只有当服务端的**应用程序**真正处理完毕并调用 close() 后，服务端才会发出 FIN 包。



如果面试官追问：**“有没有可能变成三次？”**

你可以回答：
“**在特定情况下是有可能的。**
如果服务端在收到客户端 FIN 的时候，恰好**没有数据要发送了**，而且服务端的代码处理非常快（立刻调用 close），或者开启了 **TCP 延迟确认（Delayed ACK）** 机制。
那么服务端的 **ACK**（第2步）可能会搭上 **FIN**（第3步）的顺风车，合并成一个包发送（Piggybacking）。这时候抓包看就是三次。但在标准模型中，我们必须为处理数据预留时间，所以是四次。”



### 5、JWT令牌这一块的技术细节

![image-20251223215829446](E:\program\workspace\knowledge-base\assets\image-20251223215829446.png)

![image-20251223215901150](E:\program\workspace\knowledge-base\assets\image-20251223215901150.png)

### 6.tcp怎么保证可靠链接

![image-20251223215057862](E:\program\workspace\knowledge-base\assets\image-20251223215057862.png)

针对腾讯 WXG 的面试，回答这个问题不能像背书一样罗列知识点。

你需要**构建一个体系**，告诉面试官 TCP 的可靠性是由**“四大支柱”**共同支撑的，并且在回答中要体现出你对**“效率与可靠性权衡”**的理解（比如 SACK 和 RTT 估算）。

以下是建议的回答逻辑：

---

**回答逻辑：TCP 可靠性的四大支柱**

“TCP 作为一个提供**可靠交付**的协议，它的可靠性主要通过**校验、序号、重传、控制**这四个维度来保证的：

1. 数据校验 (Checksum) —— 保证数据是对的

这是最基础的一步。TCP 头部有一个 16 位的校验和字段。
*   **机制**：发送端对头部和数据进行计算，接收端收到后验证。
*   **作用**：如果校验失败，说明数据在传输过程中被物理链路（如路由器故障、电磁干扰）**破坏了**，接收端会直接丢弃该包，**不发送 ACK**，等待发送端超时重传。

2. 序列号与确认应答 (Sequence Number & ACK) —— 保证数据不丢、不乱

这是 TCP 的核心。
*   **序列号 (Seq)**：TCP 给发送的每一个字节都编了号。它解决了两大问题：
    1.  **乱序重排**：网络传输可能先发后至，接收端根据 Seq 重新排序。
    2.  **去重**：如果接收端收到了重复的包（比如 ACK 丢了导致重传），可以通过 Seq 判断并丢弃重复数据。
*   **确认应答 (ACK)**：接收端告诉发送端：“Seq=X 之前的数据我都收到了，你可以发 X 之后的数据了。”

3. 重传机制 (Retransmission) —— 兜底策略

如果数据真的丢了，或者 ACK 丢了，TCP 必须重传。这里有两个关键点（这是面试加分项）：
*   **超时重传 (RTO)**：
    *   发送方发数据后启动定时器，时间到了没收到 ACK 就重传。
    *   **关键点**：**RTO (Retransmission TimeOut)** 不是固定的，而是**动态计算**的。它必须略大于 **RTT (往返时间)**。如果网络波动，RTT 变大，RTO 也要跟着变大，否则会引起不必要的重传。
*   **快速重传 (Fast Retransmit)**：
    *   不需要等超时。如果连续收到 3 个重复的 ACK，说明中间有个包丢了，立刻重传。
*   **SACK (Selective ACK)**（进阶）：
    *   传统的 ACK 是“累计确认”。现代 TCP 支持 SACK，接收端可以在 TCP 头部里告诉发送端：“我收到了第 100-200，还有 300-400，但中间 200-300 没收到”。这样发送端只重传丢失的那部分，**极大提升了效率**。

4. 流量控制与拥塞控制 (Flow & Congestion Control) —— 保证环境安全

可靠性不仅是数据本身，还包括**不把接收端和网络搞崩**。
*   **流量控制 (Sliding Window)**：
    *   接收端通过告诉发送端自己的 **接收窗口 (rwnd)** 大小，控制发送速度，防止接收缓冲区溢出（丢包）。
*   **拥塞控制 (Cwnd)**：
    *   慢启动、拥塞避免等算法，防止网络链路堵死导致大量丢包。

---

结合简历（杀手锏）

> *这里要再次结合你的视频平台项目，展示你对“可靠性”在业务中的理解。*

“结合我的**在线视频平台项目**，我是这样理解 TCP 可靠性的价值的：

*   **视频上传场景**：
    当用户上传几百 MB 的视频文件时，TCP 的**序列号**和**校验和**至关重要。如果中间丢了一个字节或者乱序了，整个视频文件可能就损坏无法转码。
    同时，TCP 的**滑动窗口**机制很好的适配了用户的带宽，既能跑满上传速度，又不会因为接收端（Nginx/网关）处理不过来而导致大量丢包重传。

*   **对比思考**：
    虽然 TCP 保证了可靠，但也引入了**重传延迟**。
    所以在我的项目中，如果是**视频上传**（文件完整性第一）我坚决用 TCP；
    但如果是**直播或实时音视频**（低延迟第一），TCP 的‘超时重传’会导致画面卡顿，这时候可能会考虑 **UDP + 应用层 FEC (前向纠错)** 来牺牲一点可靠性换取实时性。”

---

面试官评价预判

*   **条理清晰**：如果你能说出“校验、序号、重传、控制”这四个词，逻辑就已经满分了。
*   **深度细节**：提到了 **RTO 是动态计算的**（这很重要，很多初学者以为是固定的），提到了 **SACK**（优化效率）。
*   **业务结合**：清晰分辨了“文件上传”和“直播”对可靠性的不同需求。



## 数据库

### 事务隔离级别？

### 可重复读会出现什么问题

### 怎么解决幻读

### 高并发怎么提高分页效率？（他应该是想问大数据量提高分页效率）



**1. 核心痛点分析**

“在大数据量下（比如 1000 万行），使用标准的 LIMIT 1000000, 10 是非常慢的。
**原因在于**：MySQL 的执行机制是‘先读取 1000010 条记录，然后抛弃前 1000000 条，只取最后 10 条’。
这意味着 MySQL 需要进行大量的**随机磁盘 I/O**（回表查询数据），会把数据库的 CPU 和 IO 打满，导致高并发下数据库雪崩。”

**2. 优化方案一：利用覆盖索引进行‘延迟关联’**

“如果必须使用页码跳转（比如跳到第 10000 页），我们可以利用**覆盖索引 (Covering Index)** 来减少回表次数。

- **优化前**：SELECT * FROM video LIMIT 1000000, 10; (千万次次回表)

- **优化后**：

  codeSQL

  ```
  SELECT v.* 
  FROM video v
  JOIN (
      SELECT id FROM video LIMIT 1000000, 10
  ) tmp ON v.id = tmp.id;
  ```

- **原理**：子查询 SELECT id ... 只扫描索引树（B+ 树叶子节点只存 ID），不需要回表查具体字段，速度极快。拿到 10 个 ID 后，再通过主键关联去查具体数据，相当于只回了 10 次表，效率提升成百上千倍。”

**3. 优化方案二：游标法 / Seek Method（结合项目：视频 Feeds 流）**

> **这是腾讯面试官最想听到的，因为它对应微信朋友圈的场景。**

“在我的**微服务视频平台**中，针对**首页推荐流**这种‘无限滚动’的场景，我彻底抛弃了 LIMIT offset，而是采用了**游标法 (Cursor-based Pagination)**。

- **原理**：不记页码，只记**‘上一页最后一条数据的 ID’**（或者时间戳）。

- **SQL**：

  codeSQL

  ```
  -- 假设上一页最后一条的 ID 是 1000000
  SELECT * FROM video 
  WHERE id > 1000000 
  LIMIT 10;
  ```

- **优势**：

  - **性能 O(1)**：无论翻到多深，MySQL 都可以直接通过主键索引定位到 id=1000000 的位置，然后顺着链表往下取 10 条即可。
  - **数据稳定**：在翻页过程中，如果中间有新视频插入或删除，LIMIT offset 可能会导致数据重复或漏显，而游标法天然解决了这个问题。

- **结合项目**：我的 App 只有‘下一页’，没有‘跳到第几页’，非常适合这种方案。”





### 说说Java的乐观锁和悲观锁



**核心概念：这是两种看待“并发冲突”的态度**

“悲观锁和乐观锁本质上不是 Java 语言特有的东西，而是并发编程中的两种**冲突处理策略**：

- **悲观锁 (Pessimistic Lock)**：
  - **心态**：‘总有刁民想害朕’。它假设数据发生冲突的概率很高，所以每次读写数据前**必须先上锁**，其他线程只能阻塞等待。
  - **适用场景**：**写多读少**的场景，因为冲突频繁，阻塞等待比反复重试的开销要小。
- **乐观锁 (Optimistic Lock)**：
  - **心态**：‘天下无贼’。它假设数据发生冲突的概率很低，所以**不上锁**直接执行，只在提交更新的时候**检测**一下数据有没有被别人改过（通常比较版本号或旧值）。
  - **适用场景**：**读多写少**的场景，能避免上下文切换带来的开销，吞吐量高。”

------



2. Java **中的悲观锁实现**

“在 Java 中，悲观锁的典型代表是 **synchronized** 和 **ReentrantLock**。

- **原理**：
  - 它们底层依赖于操作系统的 Mutex Lock（互斥量）或 Java 的 **AQS (AbstractQueuedSynchronizer)** 队列。
  - 当获取锁失败时，线程会被挂起（Blocked），这涉及到**用户态到内核态的切换**，开销是比较大的（虽然 JDK 1.6 对 synchronized 做了偏向锁/轻量级锁的优化，但最终竞争激烈时还是会升级为重量级锁）。
- **结合项目**：
  在我的**校园交易系统**中，涉及到‘最终生成订单’这种必须强一致性的写入操作时，MySQL 的 SELECT ... FOR UPDATE 或者 Java 的事务注解本质上就是在使用悲观锁，确保数据绝对安全。”

------



3. **Java 中的乐观锁实现（CAS）—— 重点！**

> *这里要重点讲，因为你简历里提到了 JUC 和 CAS 思想的分布式锁。*

“Java 中乐观锁最典型的实现就是 **JUC (java.util.concurrent)** 包下的原子类，比如 AtomicInteger。它们的底层是基于 **CAS (Compare And Swap)** 算法实现的。

- **CAS 原理**：
  CAS 包含三个操作数：内存位置 (V)、预期原值 (A) 和新值 (B)。
  只有当内存位置的值等于预期原值时，才将该位置更新为新值。
  这个操作在硬件层面（x86 指令集）对应 lock cmpxchg 指令，是**CPU 原语支持的原子操作**，不需要操作系统介入，所以极快。
- **乐观锁的问题**：
  1. **ABA 问题**：一个值原来是 A，变成了 B，又变成了 A。CAS 会觉得它没变。解决方案是加**版本号**（如 AtomicStampedReference）。
  2. **CPU 开销（自旋）**：如果竞争太激烈，CAS 一直失败就会一直重试（自旋），导致 CPU 飙升。此时反而不如用悲观锁把线程挂起。
- **结合项目（亮点）**：
  我在简历中提到的**‘手写分布式锁’**，正是利用了乐观锁的 CAS 思想。
  我没有让获取锁失败的线程直接阻塞（避免线程切换），而是设计了一个**‘自旋重试’**机制：循环尝试去 Redis 抢占 Key（SetNX），配合自动过期时间，这本质上就是一种在分布式环境下的乐观锁实现。”

------



**4. 面试官追问预判：**

- *问：LongAdder 了解吗？为什么比 AtomicLong 快？*
  - *答：* LongAdder 是 Java 8 引入的。AtomicLong 在高并发下所有线程都在 CAS 更新同一个变量，竞争激烈。LongAdder 采用了**分段锁/分散热点**的思想（类似 ConcurrentHashMap），内部维护一个 Cell 数组，不同线程更新不同的 Cell，最后取总和。适合高并发计数场景。
- *问：在数据库层面，你怎么实现乐观锁？*
  - *答：* 给表加一个 version 字段。更新时：UPDATE table SET stock=stock-1, version=version+1 WHERE id=1 AND version=old_version。如果影响行数为 0，说明被别人改过了，业务层进行重试。





### 说说数据库的索引是如何实现的

![image-20251224134723392](E:\program\workspace\knowledge-base\assets\image-20251224134723392.png)

**1. 核心数据结构：B+ 树**
在 MySQL 最常用的 InnoDB 引擎中，索引是基于 **B+ 树** 实现的。这是一种多路平衡查找树，它的设计初衷是最大限度地减少磁盘 I/O 次数。

**2. B+ 树的三个关键特性（这也是为什么选它而不选 B 树的原因）**

- **非叶子节点只存索引**：B+ 树的非叶子节点（内部节点）只存储键值（Key）和指针，不存储具体数据。这使得每个磁盘页（Page，默认 16KB）能容纳更多的索引项。这大大提升了树的**扇出（Fan-out）**，使得树变得“矮胖”。通常 3 层高的 B+ 树就能存下 2000 多万行数据，这意味着查询只需要 3 次磁盘 I/O。
- **叶子节点存储所有数据**：所有的数据记录（或主键值）都只存放在叶子节点中，并且保证了查询性能的稳定性（所有查询都要走到叶子节点）。
- **双向链表连接**：所有的叶子节点通过**双向链表**按顺序连接。这使得 B+ 树在处理**范围查询**（如 BETWEEN、>）时非常高效，只需要找到起点，然后顺序遍历链表即可，利用了磁盘的顺序读特性。

**3. 物理存储分类：聚簇索引与二级索引**
在 InnoDB 中，索引的实现方式根据数据存储位置分为两类：

- **聚簇索引 (Clustered Index)**：通常就是主键索引。它的叶子节点存储的是**完整的行数据**。也就是说，索引即数据，数据即索引。
- **二级索引 (Secondary Index)**：也叫辅助索引。它的叶子节点存储的是**索引列的值**和**对应的主键值**。因此，通过二级索引查询时，如果不需要的数据不在索引里，需要拿主键去聚簇索引里再查一遍，这个过程叫**回表**。



**1. Memory 引擎与 Hash 索引**
Memory 引擎（以前叫 Heap 引擎）默认使用的是 **Hash 索引**。

- **核心优势**：它的查询效率是 **O(1)** 的。对于精确匹配查询（如 = 或 IN），由于不需要像 B+ 树那样从根节点一层层往下找，它的速度极快，理论上优于 B+ 树。
- **适用场景**：它非常适合用于存储临时数据或缓存数据（类似于 Redis 的一部分功能），比如存储会话信息或字典表。
- **局限性**：这也是为什么通用业务不用它的原因。Hash 索引**不支持范围查询**（如 >、<、BETWEEN），因为 Hash 值的分布是无序的；同时它也**不支持排序**和**最左前缀匹配**（部分索引匹配）。且 Memory 引擎的数据存储在内存中，一旦重启数据就会丢失。

**2. 进阶：InnoDB 的“自适应 Hash 索引” (Adaptive Hash Index, AHI)**
这是一个能体现你对 InnoDB 底层理解深度的点。虽然 InnoDB 主体是用 B+ 树，但它有一个“自适应 Hash 索引”机制。

- **原理**：当 InnoDB 监控到某些索引值被频繁访问（热点数据）时，它会在内存中自动基于 B+ 树构建一个 Hash 索引。
- **作用**：这样下次查询这些热点数据时，就可以跳过 B+ 树的层级遍历，直接通过 Hash 定位，从而让 B+ 树也具备了 Hash 索引 O(1) 的查询能力。这是 MySQL 内部的一个自动优化机制。

**3. 对比 MyISAM 引擎**
虽然 MyISAM 也使用 B+ 树，但它的实现和 InnoDB 有本质区别。

- **非聚簇索引**：MyISAM 的**索引文件**（.MYI）和**数据文件**（.MYD）是分离的。索引的叶子节点存储的是**数据记录的物理地址**（指针），而不是数据本身。
- **对比**：这意味着 MyISAM 的主键查询和二级索引查询没有区别，都需要一次“回表”（通过指针找数据），而在 InnoDB 中主键查询是可以直接拿到数据的。





### 讲下SQL优化思路



**第二步：分析诊断（执行计划 Explain）**
找到慢 SQL 后，我会使用 EXPLAIN 命令来分析它的执行计划。我重点关注这几个核心字段：

- **type**：这是最重要的指标。如果是 ALL（全表扫描），必须优化。我追求的目标通常是 ref（普通索引查找）或 range（范围查找），最理想是 const（主键查找）。
- **key**：确认 MySQL 实际使用了哪个索引，有没有走错索引（比如没走联合索引，而是走了单列索引）。
- **Extra**：这是“报警器”。如果出现 Using filesort（文件排序）或 Using temporary（临时表），说明查询涉及到大量的内存或磁盘操作，性能会很差，通常需要通过**联合索引**来解决排序和分组问题。

**第三步：战术层面的优化（索引与语法）**
这是最常用的手段，结合我的项目经验，主要有以下几点：

- **遵循最左前缀法则**：在我的**视频平台**项目中，我有 (create_time, category, user_id) 的联合索引。查询时，我必须保证查询条件从 create_time 开始，中间不能断（比如不能跳过 category 直接查 user_id），否则索引失效。
- **利用覆盖索引 (Covering Index)**：尽量只 SELECT 需要的字段，而不是 SELECT *。如果查询的字段（如 id, title）恰好都在索引树上，MySQL 就不用回表去聚簇索引查数据，I/O 开销会降低一半以上。
- **避免索引失效的坑**：不在索引列上做计算或函数操作（如 DATE(create_time)），不使用 !=，字符串查询一定加引号，模糊查询 LIKE 不以 % 开头。
- **优化分页**：针对大数据量的深分页（LIMIT 1000000, 10），我不再使用 Offset，而是使用 **“延迟关联”**（先查 ID 再回表）或者 **“游标法”**（记录上一次的 MaxID），这在前面关于分页的回答中详细提到过。





### 讲一下数据库中的联合索引的底层实现和使用限制



**一、 底层实现原理：多列排序的 B+ 树**

首先，**联合索引底层依然是 B+ 树**。它并不是为每一列建一棵树，而是**多列共用这一棵树**。

假设我们有一个联合索引 (a, b, c)，它的底层存储结构具有以下特征：

1. **节点存储**：
   B+ 树的节点中存储的 Key 不再是单一的 a，而是一个元组 (a, b, c)。
   对于非叶子节点，存储的是索引键值；对于叶子节点（二级索引），存储的是 (a, b, c) 加上 **主键 ID**。
2. **排序规则（核心核心核心！）**：
   这是理解联合索引的关键。B+ 树在构建时，遵循**“全局有序，局部相对有序”**的原则：
   - **第一优先级**：先严格按照字段 a 从小到大排序。
   - **第二优先级**：只有在 a 的值**相等**时，才会按照 b 排序。
   - **第三优先级**：只有在 a 和 b 都相等时，才会按照 c 排序。



**使用限制：最左前缀原则 (Leftmost Prefix Principle)**

基于上述的排序结构，衍生出了联合索引最著名的使用限制——**最左前缀原则**。

**1. 必须从最左侧开始匹配**
假设索引是 (a, b, c)：

- WHERE a=1 ✅：因为 a 是全局有序的，可以走索引。
- WHERE b=2 ❌：因为跳过了 a，在全局看 b 是乱序的，无法走索引（全表扫描）。
- WHERE c=3 ❌：同理。

**2. 遇到范围查询会停止匹配**
这是面试常考的坑点。

- **SQL**：WHERE a=1 AND b>2 AND c=3
- **分析**：
  - a=1：走索引，定位到 a=1 的区域。
  - b>2：走索引，因为在 a=1 的前提下，b 是有序的。
  - c=3：**不走索引**。
- **原因**：当 b 是范围查询（>2）时，选出的结果集可能是 (1, 3, x), (1, 4, y), (1, 5, z)。在这些结果中，c 的值可能是乱序的，B+ 树无法利用索引进行二分查找。



### Innodb为什么选择B+树，预读顺序IO要考虑一下



**1. 核心矛盾：磁盘 I/O 的昂贵代价**
数据库的性能瓶颈通常不在 CPU，而在磁盘 I/O。磁盘的**随机 I/O**（Random I/O）非常慢，因为需要磁头寻道和旋转延迟；而**顺序 I/O**（Sequential I/O）很快，甚至可以接近内存的传输速度。InnoDB 选择 B+ 树的根本原因，就是要**利用“页存储”和“局部性原理”来最大限度地利用顺序 I/O 和预读机制**。

**2. B+ 树结构对顺序 I/O 的极致优化**
这一点是 B+ 树战胜 B 树的关键。
在 B 树中，数据是分散存储在各个节点（包括根节点和内部节点）中的。如果你要进行范围查询（比如 SELECT * FROM table WHERE id > 100）：

- B 树需要进行中序遍历，这就意味着指针可能会在父节点、子节点之间反复横跳。物理上，这些节点在磁盘上可能相隔很远，导致大量的**随机 I/O**。
- B+ 树的所有数据都紧凑地存储在**叶子节点**，并且叶子节点之间通过**双向链表**逻辑相连。当找到起点后，数据库只需要顺着链表向后读取即可。这在物理磁盘上往往对应着连续的存储块，从而将大量的随机读取转化为高效的**顺序读取**。

**3. 充分利用操作系统的“预读” (Read-Ahead) 机制**
操作系统和磁盘硬件在读取数据时，并不是需要几个字节就读几个字节，而是以**页 (Page)** 为单位（通常 4KB，InnoDB 默认 16KB）。

- **空间局部性原理**：当程序访问某个数据时，它附近的数据也很可能马上被访问。

- **预读机制**：基于局部性原理，当你读取磁盘的第 

  ```
  NN
  ```

   页时，操作系统会预测你可能会读第 

  ```
  N+1N+1
  ```

   页，于是会悄悄把 

  ```
  N+1N+1
  ```

   页也一起读进内存（Page Cache）。

  

- **B+ 树的配合**：由于 B+ 树的叶子节点存储了大量连续的数据行，且逻辑相邻的页在磁盘分配时也尽量物理相邻。当我们在 B+ 树上进行范围扫描时，操作系统的预读机制能完美生效，提前把下一页数据加载进内存。而 B 树因为数据分散，跨层跳转多，破坏了数据的局部性，导致预读失效（读进来的数据可能根本用不上）。





### 5.聚簇索引和非聚簇索引有啥区别。



**1. 核心区别：叶子节点存储的内容不同**
这是最本质的区别。

- **聚簇索引 (Clustered Index)**：通常就是**主键索引**。
  - 它的叶子节点存储的是**整行数据 (Row Data)**。
  - 也就是说，**数据本身就是索引的一部分**。找到了索引，就找到了数据。
- **非聚簇索引 (Non-Clustered Index)**：也叫二级索引或辅助索引。
  - 它的叶子节点存储的是**索引列的值**以及**对应的主键值 (Primary Key)**。
  - 它不存完整的行数据。

**2. 数量限制不同**

- **聚簇索引**：一张表**只能有一个**。因为数据在磁盘上的物理（逻辑）存储顺序只能有一种。
- **非聚簇索引**：一张表**可以有多个**。我们可以根据业务需求，建立 create_time、category 等多个辅助索引。

**3. 查询效率与“回表” (Key Performance Indicator)**
这是面试官最关心的性能点。

- **查聚簇索引**：速度最快。例如 SELECT * FROM user WHERE id = 1，B+ 树只需一次遍历找到叶子节点，就能直接拿到所有数据。
- **查非聚簇索引**：
  - **情况 A（需要回表）**：如果执行 SELECT * FROM user WHERE name = 'Tom'。
    1. 先在“name 索引树”上找到 name='Tom' 的节点，拿到主键 ID。
    2. 再拿这个 ID 去“聚簇索引树”上再查一次，才能获取整行数据。
    3. 这个过程叫**回表 (Look up)**，多了一次 B+ 树扫描，性能会有损耗。
  - **情况 B（覆盖索引优化）**：如果执行 SELECT id FROM user WHERE name = 'Tom'。
    1. 在“name 索引树”上直接就能拿到 ID，**不需要回表**。这是 SQL 优化的核心技巧。

**4. 对比 MyISAM 引擎（进阶补充）**
值得一提的是，上述区别主要针对 **InnoDB**。
如果是在 **MyISAM** 引擎中，**所有的索引都是非聚簇索引**。

- MyISAM 的数据文件 (.MYD) 和索引文件 (.MYI) 是分离的。
- 主键索引和二级索引的叶子节点存的都是**物理地址指针**。
- 所以在 MyISAM 中，主键查询和二级索引查询的路径是一样的，都需要“回表”去数据文件中拿数据。



讲一下各种隔离级别以及隔离级别下面可能的问题(脏读不可重复读幻读)，mysql在RR下面如何避免幻读，什么情况仍然会幻读?



数据库标准定义了四个隔离级别，级别越高，安全性越高，但性能越低。MySQL InnoDB 默认使用的是 **RR (Repeatable Read)**。

- **Read Uncommitted (读未提交)**
  - **机制**：事务可以读取其他事务未提交的数据。
  - **问题**：**脏读 (Dirty Read)**。事务 A 修改了数据但未提交，事务 B 读到了这个“脏数据”。如果 A 回滚了，B 读到的数据就是无效的。
- **Read Committed (读已提交, RC)**
  - **机制**：事务只能读取已经提交的数据。
  - **解决**：解决了脏读。
  - **问题**：**不可重复读 (Non-repeatable Read)**。事务 A 第一次读 id=1 是 100 岁，此时事务 B 把 id=1 改成了 200 岁并提交。事务 A 第二次读就变成了 200 岁。同一个事务内两次读取结果不一致。
- **Repeatable Read (可重复读, RR)**
  - **机制**：保证在同一个事务中多次读取同一条记录的结果是一致的。
  - **解决**：解决了脏读、不可重复读。
  - **问题**：标准 SQL 定义中存在 **幻读 (Phantom Read)**。
  - **幻读定义**：事务 A 查询“所有 18 岁的用户”，发现有 10 人；此时事务 B **插入**了一个新的 18 岁用户并提交；事务 A 再次查询（或进行更新操作），发现变成了 11 人。**不可重复读重点在于 update/delete（值的变化），幻读重点在于 insert（数量的变化）。**
- **Serializable (串行化)**
  - **机制**：强制事务串行执行。
  - **解决**：解决所有问题，但性能极差，几乎不用



### mysql索引失效情况  




14.为什么使用函数会导致索引失效

  19、什么情况下考虑建索引，什么样的字段建索引 



count(distinct column) / count(*) 越接近 1，越适合建索引

![image-20251224142142503](E:\program\workspace\knowledge-base\assets\image-20251224142142503.png)

  20、如果某个字段的索引区分度很低，查询很慢，怎么办 

  21、存储过程是什么，为什么快 

22、有没有统计过美团外卖的浏览量和下单的频率（就是读操作和写操作频率的比较~~）

## java

接口安全性如何保证？

写接口时有没有考虑到性能问题？

怎么去实现一个接口幂等的？

ThreadLocal原理，是怎么确定是哪个线程？

springboot中除了注解，还能去怎么开启事务？

什么时候会发生嵌套事务？

Bean作用域（这个真忘记了哈哈，只记得一个单例）

线程池怎么实现的 -- 具体的实现executor是executor实例化executor类，设置相关七大参数

线程池的具体工作流程是怎样的 

Java Map有哪些实现？HashMap底层是什么数据结构，有什么好处，什么缺点？TreeMap底层是什么数据结构，有什么好处？

哈希表有哪些解决冲突的方案？（讲了开放寻址、链地址、二次散列）

equals和hashcode的关系，深入分析一下

 除了uuid，还有哪些生成唯一id的方法？

说一下各种排序算法，稳定性如何?快排能写吗

## JVM

结合JVM运行时内存区域，说说volatile关键字

Java GC咋做的

java内存泄露怎么排查  

## 中间件

 消息队列消息丢失怎么办，补偿机制？

 redis分布式锁与zookeeper的区别 

rocketmq事务消息怎么实现的

rocketmq是怎么存储的  ，存储做了哪些优化 

16.es怎么实现模糊查询，以及评分机制
17.mysql使用场景
18.乐观锁和悲观锁

10.redis怎么实现分布式🔒

使用redis之后，更新操作是怎么处理的