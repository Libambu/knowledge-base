# 计网

## http和https的区别

http1.0 2.0 3.0

这是一个非常经典的计算机网络面试题。面试官通常希望听到你从**“性能瓶颈”**的角度去描述它们的演进过程。

为了逻辑连贯，我必须在 1.0 和 2.0 之间补充一个 **HTTP/1.1**，因为它是目前使用最广泛的版本，也是理解 2.0 优化的基础。

以下是核心区别的通俗解释和详细对比：

---

### 1. HTTP/1.0：短连接时代 (The Stone Age)
**核心特征：一问一答，用完即弃。**

*   **短连接 (Short Connection)**：这是最大的痛点。浏览器每请求一个资源（比如一张图片），都要和服务器建立一次 TCP 连接（三次握手），传输完立刻断开（四次挥手）。
*   **性能极差**：如果网页有 100 张图片，就要建立 100 次连接。
*   **无 Host 头**：认为一台服务器只有一个 IP，不支持虚拟主机（Virtual Hosting）。

---

### 2. HTTP/1.1：持久连接时代 (The Standard Era)
*这是 HTTP 1.0 的改进版，也是目前互联网的基石。*

*   **长连接 (Keep-Alive)**：默认开启 `Connection: keep-alive`。建立一次 TCP 连接后，可以发送多次请求，不用反复握手。
*   **管道化 (Pipelining)**：允许在还没收到响应前发送下一个请求。
*   **致命缺陷：队头阻塞 (Head-of-Line Blocking)**
    *   虽然可以复用连接，但服务器处理是**串行**的。
    *   如果第一个请求处理很慢（比如查大数据库），后面的请求（比如加载 CSS）就全堵在后面等着，导致页面加载白屏。

---

### 3. HTTP/2.0：多路复用时代 (The Performance Era)
**核心变革：解决 HTTP 应用层的队头阻塞。**

*   **多路复用 (Multiplexing) —— 最重要的功能**
    *   在 HTTP/1.1 中，浏览器往往要开 6-8 个 TCP 连接来并发下载。
    *   在 HTTP/2.0 中，**只需一个 TCP 连接**。在这个连接里，可以同时传输多个请求和响应。它们被打散成“帧”，乱序发送，互不干扰。
*   **二进制传输 (Binary Framing)**
    *   HTTP/1.x 是纯文本（Text），解析慢且容易出错。
    *   HTTP/2.0 是二进制（Binary），机器解析更高效。
*   **头部压缩 (HPACK)**
    *   HTTP/1.x 每次请求都要带一大堆重复的 Header（如 `User-Agent`, `Cookie`）。
    *   HTTP/2.0 使用 HPACK 算法，双方维护一个索引表，重复的头只需要传索引号，极大减少流量。
*   **服务端推送 (Server Push)**
    *   浏览器请求 `index.html`，服务器知道你肯定需要 `style.css`，于是主动把 CSS 推给你，不用你再发请求。

**2.0 的遗留问题：**
虽然解决了 HTTP 层的队头阻塞，但**TCP 层的队头阻塞**依然存在。因为 TCP 是保证可靠按序传输的，如果底层丢失了一个 TCP 数据包，操作系统会把整个连接暂停，等待重传，这会拖累所有流。

---

### 4. HTTP/3.0：QUIC 时代 (The Future)
**核心变革：抛弃 TCP，拥抱 UDP。**

Google 意识到 TCP 协议太老了，改不动内核，于是基于 **UDP** 搞了一个新协议叫 **QUIC**，后来变成了 HTTP/3。

*   **彻底解决队头阻塞**
    *   因为 UDP 不需要按序到达。在 QUIC 中，Stream A 丢包了，只需要重传 A，Stream B 和 C 照样可以正常读取，互不影响。
*   **更快的建连 (0-RTT / 1-RTT)**
    *   TCP+TLS 需要握手多次才能发数据。
    *   HTTP/3 基于 QUIC，可以在握手的同时就开始发送数据（甚至 0-RTT），网页打开速度极快。
*   **连接迁移 (Connection Migration)**
    *   **场景：** 你用手机连着 Wi-Fi 看视频，突然出门切换到了 4G。
    *   **TCP：** IP 变了，连接断开，视频卡顿，需要重新建立连接。
    *   **QUIC：** 不靠 IP 端口识别连接，而是用一个 **Connection ID**。只要 ID 没变，就算你从 Wi-Fi 切到 4G，连接依然保持，视频不卡顿。

---

### 总结对比表 (面试背这个)

| 特性           | HTTP/1.0          | HTTP/1.1            | HTTP/2.0                    | HTTP/3.0                     |
| :------------- | :---------------- | :------------------ | :-------------------------- | :--------------------------- |
| **传输层协议** | TCP               | TCP                 | TCP                         | **UDP (QUIC)**               |
| **连接方式**   | 短连接 (Short)    | 长连接 (Keep-Alive) | 多路复用 (Multiplexing)     | 多路复用 (QUIC Streams)      |
| **数据格式**   | 文本              | 文本                | **二进制 (Binary)**         | 二进制                       |
| **队头阻塞**   | 严重 (每次都要连) | 有 (HTTP层阻塞)     | **无 HTTP阻塞，有 TCP阻塞** | **彻底无阻塞**               |
| **头部压缩**   | 无                | 无                  | HPACK                       | QPACK                        |
| **网络切换**   | 断线重连          | 断线重连            | 断线重连                    | **平滑迁移 (Connection ID)** |

**一句话总结演进逻辑：**
*   **1.0 -> 1.1**：为了复用连接（省去握手时间）。
*   **1.1 -> 2.0**：为了并发传输（解决 HTTP 层的排队等待）。
*   **2.0 -> 3.0**：为了摆脱 TCP 的包袱（解决丢包导致的全局卡顿，适应移动网络切换）。




这是一个非常标准且优秀的面试回答模板。回答的逻辑采用**“演进史”**的方式，即：**前一个版本有什么痛点 -> 后一个版本是怎么解决的**。这样不仅条理清晰，还能体现你对底层原理的理解。

建议语速适中，按照以下话术进行回答：

------



### 面试回答话术模板

**开场白：**
“面试官你好，HTTP 协议的演进史，本质上就是一部不断**优化网络性能**和**降低延迟**的历史。我可以从 1.0 到 3.0 的核心痛点和解决方案来为您梳理。”

**第一阶段：HTTP 1.0 到 1.1（从短连接到长连接）**
“首先是 1.0 版本，它最大的缺陷是**短连接**。每次发送请求都要重新进行 TCP 三次握手，传输完就断开，性能损耗非常大。
为了解决这个问题，HTTP/1.1 引入了 **Keep-Alive 长连接**机制，允许在同一个 TCP 连接上发送多个请求，复用了连接。
**但是**，1.1 虽然复用了连接，请求的处理依然是**串行**的。如果前一个请求处理很慢（比如大图或者查库），后面的请求就必须排队等待，这被称为 **HTTP 层面的队头阻塞**。”

**第二阶段：HTTP 1.1 到 2.0（多路复用解决 HTTP 阻塞）**
“为了解决 1.1 的串行阻塞问题，HTTP/2.0 做了巨大的底层重构。
核心区别在于，2.0 采用了**二进制传输**，引入了**多路复用（Multiplexing）\**技术。
它将请求拆分成二进制帧，允许在\**同一个** TCP 连接中，并发地发送多个请求和响应，互不干扰。这彻底解决了 HTTP 层的队头阻塞。
此外，2.0 还引入了 **HPACK 头部压缩**（解决 Header 重复传输浪费流量的问题）和**服务端推送**功能。”

**第三阶段：HTTP 2.0 到 3.0（抛弃 TCP，拥抱 QUIC）**
“虽然 2.0 表现很好，但它底层依然基于 TCP。TCP 是可靠传输协议，一旦发生**丢包**，操作系统会暂停整个连接来等待重传，导致连接里所有的流都被阻塞，这被称为 **TCP 层面的队头阻塞**。这是 TCP 协议自身的物理瓶颈。
所以，HTTP/3.0 决定**抛弃 TCP**，改用基于 UDP 的 **QUIC 协议**。
QUIC 的核心优势是：即使某个流丢包了，也只影响那个流，其他流依然能正常传输。
此外，3.0 还实现了 **0-RTT 快速建连**（握手速度极快）以及基于 Connection ID 的**平滑网络迁移**（比如从 WiFi 切到 4G 不断连）。”





## 介绍TCp模型四层结构，以及每一层的作用

“TCP/IP 模型分为四层，每一层各司其职：

- **应用层**：负责具体的业务应用，比如 HTTP 协议，解决‘传输什么内容’的问题。
- **传输层**：负责端到端的传输，利用 TCP/UDP 协议和端口号，解决‘发给哪个软件’的问题。
- **网络层**：负责路由寻址，利用 IP 协议，解决‘怎么找到对方’的问题。
- **网络接口层**：负责物理传输，利用 MAC 地址和网卡，解决‘怎么把数据发出去’的问题。



解释下https里tls的非对称加密怎么搞的

.tcp连接中time_wait状态什么时候发生的?

## http，https，tcp，udp的联系



“简单来说，它们的关系是**底层基础与上层建筑**的关系。

**HTTP/HTTPS 位于应用层**，负责定义数据的格式和语义；而 **TCP/UDP 位于传输层**，负责数据的实际搬运。

传统的 **HTTP 和 HTTPS 是基于 TCP** 运行的，因为 TCP 提供的三次握手、丢包重传和拥塞控制能保证网页数据传输的完整性和准确性。HTTPS 则在 TCP 基础上增加了 SSL/TLS 加密层，保证了安全性。

**TCP 和 UDP 的主要区别**在于可靠性与速度的权衡。TCP 像打电话，必须确认连接才发数据，虽然慢一点但稳；UDP 像发广播，不保证对方收到，但速度极快。

**值得注意的是**，随着技术演进，最新的 HTTP/3 为了解决 TCP 建立连接慢的问题，改用了基于 UDP 的 QUIC 协议，这说明应用层协议正在通过优化传输层选择来追求更高的性能。”







## 如果大量的连接停留在close_wait说明了什么如果大量的连接停留在time_wait说明了什么

“这两个状态都出现在 TCP 的四次挥手阶段，但代表的意义完全不同。

**大量 CLOSE_WAIT 停留在被动关闭方**。
这说明**程序出 Bug 了**。对方发了断开请求（FIN），我方内核回了 ACK，但应用层代码迟迟没有调用 close() 函数来关闭 socket。这通常是资源泄露，比如代码没写 finally { socket.close() }。危害是会耗尽文件描述符，导致系统无法处理新请求。

**大量 TIME_WAIT 停留在主动关闭方**。
这是一个**正常机制**。它是挥手后的最后一个状态，会持续 **2MSL** 时间，目的是为了确保最后一个 ACK 到达对方，并防止旧连接的‘残余报文’干扰新连接。
如果出现大量堆积，说明系统处理了**极高频率的短连接**（比如爬虫或没开 Keep-Alive）。危害是会占满本地端口，导致新连接发不出去。解决办法是改用**长连接**，或者在内核层面开启 tcp_tw_reuse（端口重用）。”





# redis

redis常用数据结构
redis哈希怎么存储的，怎么扩容，复制时如何保证读数据

Zset的底层实现

回到Redis，Redis为什么快？

缓存击穿中的数据具体是什么数据，为什么是这些数据，有多大

.为什么用redis作为缓存？为什么它快？
6.除了你说的基于内存和单线程和多路复用还有什么快的原因？

# 数据库

mysql事务四性
四性如何保证的

MySQL的explain指令结果有哪些字段 + type类型一共有哪些
联合索引失效的原因

mysql中一个表数据量大，查询慢，如何优化

mysql索引，作用，原理，数据结构，为什么用B+树，还有哪些数据结构

有哪些数据表，哪些字段，如何设计的

产品上线后，有一天发生缓存雪崩了导致sql宕机服务宕机，怎么立马解决

# JVM

jvm内存模型
类里新的实例存哪里?应该是堆，答栈了栈?怎样让他存堆?死性不改没有听懂潜台词，继续错



# 操作系统

虚拟内存和物理内存的区别

介绍一下epoll的底层实现 + 水平触发和边缘触发的区别 + 采用边缘触发需要注意什么

1. 说说IO多路复用三种方式（select/poll/epoll，感觉我说的不太好）

# 算法

LC 380 O(1) 时间插入、删除和获取随机元素

经典的几十亿个int值去重问题

# java

 ArrayList是否线程安全 举例说明 并怎么解决

讲讲数据结构
2.数组与链表区别，如何选择
3.hashmap，hashtable实现原理
4.hashmap哈希冲突高，遍历久，如何处理
5.哈希表的其他设计方案，除了使用数组+链表
6.除了链表/红黑树，还有没有其他方法解决哈希冲突



一面，基本都在拷打实习，已经面成老油条了，自然不在话下，最后问了点简单八股也答上来了。
9.22 二面，没问啥技术问题，基本都在聊技术人员的产品思维，我说了说实习期间自己的产品思维，这面试官有意思。
9.26 HR面，没啥好说的，🐧还是一如既往的亲切。

作者：offer自动机
链接：https://www.nowcoder.com/feed/main/detail/338e3fb0f4784281b359d19bff1da992?sourceSSR=search
来源：牛客网



# 智力

.无限水，两个容器，容量为5L、3L，如何获得4L水 2.100本书，两个人轮流拿，每次拿1-5本，你先拿，如何保证拿到最后一本